# Текущее задание

> **Как начать новую сессию** (владельцу — чтобы не помнить наизусть). Скопировать это:
>
> Работаем по editor/TASKS.md. Возьми текущее задание и точку продолжения из раздела 0
> editor/CURRENT_TASK.md и начни строго по протоколу. Сначала прочитай корневой CLAUDE.md, затем
> editor/CURRENT_TASK.md и сверь указанный там снимок HEAD и рабочего дерева. Не додумывай
> отсутствующие решения и не расширяй границы задания. Если состояние репозитория не соответствует
> допустимому снимку или для продолжения требуется решение владельца — остановись и сообщи точное
> расхождение.

> **Это контракт текущего шага, а не архив.** Предел — пара сотен строк. История кругов живёт в
> `JOURNAL.md`, решения и их обоснования — в `DECISIONS.md`, задания и правила работы — в
> `TASKS.md`. Указатель, где что искать, — в конце файла.

---

## 0. Где мы сейчас

**Задание З0 из `TASKS.md`** — файл состояния строится от имени файла версии, а не от папки.
Задание разбито на два шага его же порядком.

- **Шаг 1 — команда корпусного прогона. ЗАКРЫТ** 2026-08-12: `npm run editor:corpus`, отметка на
  текущем коде нулевая (81 файл контента, ни одного изменившегося). Одиннадцать кругов ворот,
  последний чистый. Разбор стоимости этого шага и отступление от протокола — в `JOURNAL.md`.
- **Шаг 2 — правило имени файла состояния. ЗАКРЫТ** 2026-08-13. Записка прошла три круга, код —
  круги ворот до круга без находок. Машина: 1022 теста, типы чисто, корпусный прогон нулевой и
  совпал с отметкой шага 1. Живая проверка в окне пройдена: пробная статья-файл в «Уроках»
  сохранена, у неё своё состояние, страница раздела осталась «Опубликована», общий `_state.json`
  не создан. Круги и доказательства — в `JOURNAL.md`.

**Задание З0 закрыто целиком** (оба шага). Следующее задание берётся из `TASKS.md` — З1а.

**Класс и границы следующего задания здесь не заданы намеренно** — они берутся из листа З1а перед
первой правкой. Класс шага нельзя назначить ниже класса задания, а разбивка на шаги записывается
до кода.

**Ветка и дерево.** `main`. **Базовый коммит на момент записи — `9750e9b`** (обновление снимка в
карточке). **Последний коммит содержательной работы — коммит этой передачи**: в нём правило имени
файла состояния, два новых файла проверок и бумага по закрытому заданию З0. Сверка — по правилу
`TASKS.md` («Передача между сессиями»): `HEAD` равен базовому либо является ровно одним его
потомком, трогающим только бумажные файлы. Рабочее дерево обязано быть чистым.

---

## 1. Записка по закрытому шагу 2 — в архиве

Шаг 2 задания З0 закрыт 2026-08-13, поэтому его записка здесь больше не лежит: карточка — контракт **текущего** шага, а не архив. Что от неё осталось живого и где искать:

- **правило имени и почему «индекс» — это только точное имя `index`** — `DECISIONS.md`
  (разбор крайних случаев) и лист З0 в `TASKS.md`;
- **круги проверки, находки и доказательство чувствительности тестов** — `JOURNAL.md`,
  раздел про шаг 2;
- **закрытый хвост про общий файл готовности на папку** — `PLAN.md`, помечен закрытым.

---

## 2. Передача между сессиями: девять пунктов

1. **Задание и шаг.** З0 закрыто целиком. Следующее — **З1а, закрытие ядра подготовки**, не начато.
2. **Класс и границы.** Берутся из листа З1а: этой карточкой границы нового задания не заданы.
3. **Что доказано по З0.** Оба шага. Шаг 1 — команда корпусного прогона с нулевой отметкой. Шаг 2 —
   все критерии приёмки задания: перечень физических файлов состояния не изменился (их по-прежнему
   ноль), вычисленные пути разошлись ровно у трёх `intro.mdx` и ни у одной статьи-папки, готовность
   каждой живой статьи прежняя, пробная статья-файл в «Уроках» получила своё состояние и не тронула
   страницу раздела, корпусный прогон зелёный, круг ворот без находок.
4. **Изменённые файлы (шаг 2).** Код: `src/core/articleState.mjs`, `src/adapters/library.mjs`,
   `src/core/newArticle.mjs`. Новые тесты: `tests/statePath.test.mjs`, `tests/stateSweep.test.mjs`.
   Бумага: `CURRENT_TASK.md`, `PLAN.md`, `MAP.md`, `DECISIONS.md`, `JOURNAL.md`.
5. **Итог проверок (2026-08-13).** `editor:test` — 1022 теста, 46 файлов, зелено · `editor:typecheck`
   — чисто · `editor:corpus` — код выхода 0, 81 файл, переписан 0, разошёлся 0, только переводы
   строк 1, без шапки 0; статей 37/36/1; блокеров 19, предупреждений 1.
6. **Ветка и дерево.** См. раздел 0; правило сверки — `TASKS.md`, «Передача между сессиями».
7. **Находки вне задания — пять, все хвостами в `PLAN.md`, ни одна не чинится (шаг 4):** смешанные
   переводы строк в статье блога про бесплатные семейства · файл без шапки, начатый невидимой
   меткой, получил бы её дважды · сборка шапки в окне корпусно не проверяется вовсе · статья-файл
   подписывается в шапке «страница раздела» (З16) · удаление статьи-файла из окна либо отказывает,
   либо уносит папку раздела целиком (З13, З16). **Принятые решения шага 2:** «индекс» в правиле
   пути — только точное имя `index` (`DECISIONS.md`, разбор крайних случаев).
8. **Одно точное следующее действие.** Прочитать лист З1а в `TASKS.md` и разбить его на шаги ДО
   первой правки: задание помечено как многосессионное, а класс шага понижать нельзя.
9. **Что читать.** `TASKS.md`: З1а, «Правило проверки по риску», «Как выполняется одно задание»,
   «Словарь» · `SPEC.md` по теме подготовки · `PLAN.md`, хвосты про подготовку.

## 3. Кто проверяет

Записки и ворота ведёт агент-контролёр (`controller`): у Codex лимиты кончились до 16 августа 2026.
Живая проверка обязательна; в отчёте владельцу сказано, кто проверял.

## 4. Указатель: где что искать, чтобы не искать здесь

- **Зачем программа и что должен уметь человек** — `BUSINESS.md` (главнее всех документов).
- **Задания, порядок, приёмка, правило проверки по риску, правила работы над заданием** — `TASKS.md`.
- **Почему так решено** — `DECISIONS.md` (там же решения по поведению, проверенные советом).
- **Всё про шапку статьи** — `FRONTMATTER.md`; разобранное не переспрашивается.
- **Круги ворот, их цена и факты окружения** — `JOURNAL.md` (на старте не читается).
- **Живые недоделки** — `PLAN.md`, «Хвосты». **Код по файлам** — `MAP.md`. **Запреты** — `SPEC.md`.
- **Отчёт владельцу** — строго по `REPORT.md`; отчёт не по шаблону не считается отчётом.
