# Управление draft у каждой локали
## Результат
Владелец одним полем «Доступность на сайте» управляет открытой языковой версией без запрещённого
сочетания `draft` и `unlisted`. Состояния: «Недоступно», «По ссылке», «Доступно всем».
Старая опубликованная версия без этих полей не переписывается при открытии.

## Снимок
- board: `ED-025`; operation: `draft-control`; class: `2`; state: `selected`.
- branch: `fix/editor-draft-control`; base: `61531b189ae1666514ebe48fbf64fbcfd01e7093`.
- target: `editor`.
- worktree: `C:/My_code/bimcore-blog/editor/.coordination/worktrees/fix-editor-draft-control`.
- Риск: ошибочное значение или фоновая запись может скрыть опубликованную локаль либо создать
  ложное изменение статьи.

## Границы этой операции
- Вместо независимых флажков у локали один трёхрежимный переключатель «Доступность на сайте»:
  все три варианта постоянно видны и выбираются одним нажатием, это не выпадающий список.
- «Недоступно»: `draft: true`, строка `unlisted` отсутствует.
- «По ссылке»: `unlisted: true`, строка `draft` отсутствует; это состояние новой статьи и локали.
- «Доступно всем»: обе строки отсутствуют. Явные `false` при чтении равнозначны отсутствию.
- При выборе режима противоположное поле удаляется; запрещённая пара никогда не записывается.
- Изменение применяется только к открытой локали обычным сохранением и само ничего не публикует.
- Выбранное значение поля показывает текущее локальное состояние открытой версии, чтобы владелец
  мог подготовить следующую публикацию.
- Красный цвет букв локали означает только одно: в уже опубликованной версии на `origin/main`
  записано `draft: true`. Локальная будущая смена режима до публикации цвет букв не меняет.
- Значок ссылки означает только одно: в уже опубликованной версии на `origin/main` записано
  `unlisted: true`. Локальная будущая смена режима до публикации значок не создаёт и не убирает.
- Если опубликованную `origin/main` прочитать нельзя, красный цвет и значок ссылки молчат: локальная
  ветка или `HEAD` не могут подменять доказательство состояния сайта.
- Любое подтверждённо сохранённое отличие локальной версии от опубликованной, включая смену
  доступности, показывает существующую красную точку рядом с локалью.
- Рядом с полем доступности можно показать такую же красную точку только если она использует
  ровно тот же готовый факт отличия версии. Второй расчёт и расширение операции ради этой точки
  запрещены; её отсутствие не блокирует приёмку.
- Открытие и сохранение без выбора нового режима не нормализует и не переписывает старую статью.
- Новая обычная статья и создаваемая языковая версия получают «По ссылке».
- Не менять публикацию, готовность, миграцию корпуса, статьи, заглушки и страницы разделов.
- Не исправлять соседние визуальные или редакторские замечания.

## Приёмка и остановка
- Адресные тесты трёх режимов и всех переходов; старые `false` и отсутствие полей; независимость
  RU/EN/ES; новая статья и новая локаль.
- Для отсутствующего поля простое открытие и сохранение не меняют байты файла.
- Переключение меняет только выбранную локаль, не публикует её, не создаёт запрещённую пару и
  сохраняется после перезапуска.
- Красный цвет букв есть только при `draft: true` в опубликованной версии и одинаков в реестре и
  шапке; локальное добавление или снятие `draft` до публикации меняет только красную точку.
- Значок ссылки есть только при `unlisted: true` в опубликованной версии; локальное добавление или
  снятие `unlisted` до публикации меняет только красную точку.
- При недоступной `origin/main` ни красный цвет, ни значок ссылки не показываются как гарантия сайта.
- Проверяются независимые сочетания опубликованного и локального состояния для RU/EN/ES: старый
  опубликованный цвет/значок сохраняется до публикации, а локальная разница обозначается точкой.
- Типы, полный набор один раз, корпус чтения/записи без переписывания статей, сборка при изменении MDX,
  проба на копии статьи и итоговый контролёр класса 2.
- Исполнитель оставляет чистый контрольный коммит и точный адрес живой пробы.
Остановиться на `ready_for_review`. Полная миграция и ED-026 про страницы разделов не входят.
