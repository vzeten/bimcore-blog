# Текущее задание

> **Как начать новую сессию** (владельцу — чтобы не помнить наизусть). Скопировать это:
>
> Работаем по editor/TASKS.md. Возьми текущее задание и точку продолжения из раздела 0
> editor/CURRENT_TASK.md и начни строго по протоколу. Сначала прочитай корневой CLAUDE.md, затем
> editor/CURRENT_TASK.md и сверь указанный там снимок HEAD и рабочего дерева. Не додумывай
> отсутствующие решения и не расширяй границы задания. Если состояние репозитория не соответствует
> допустимому снимку или для продолжения требуется решение владельца — остановись и сообщи точное
> расхождение.

> **Это контракт текущего шага, а не архив.** Предел — пара сотен строк. История кругов живёт в
> `JOURNAL.md`, решения и их обоснования — в `DECISIONS.md`, задания и правила работы — в
> `TASKS.md`, порядок движения к сквозному MVP — в `MVP.md`.

---

## 0. Где мы сейчас

**Срез 2 сквозного MVP СДЕЛАН 2026-08-13: предварительный выпуск — состав файлов и полная сборка.**
Порядок работ — вертикальными срезами (переходное решение владельца, `MVP.md`).

**Чем доказан.** 1129 машинных проверок (50 файлов), типы чисто, корпусный прогон не сдвинулся.
Живая проверка целиком: состав живой статьи (три языковые версии и три обложки, ни одного чужого
файла) · зелёная сборка сайта — «можно перейти к подтверждению», кнопка подтверждения показана
запертой · красная сборка на временном файле, созданном и удалённом мной же, — «выпускать нельзя»,
кнопки подтверждения нет, вывод сборщика доступен целиком. Git не тронут ничем: `HEAD` прежний,
индекс пуст, ни одна статья не изменена.

**Срез 1 закрыт 2026-08-13, контрольная точка — коммит `8717ce9`.** «Подготовить» зовёт готовое ядро
и показывает отчёт. Дополнительно его не полируют. **Переход от строки отчёта к полю признан
удобством интерфейса, а не разрывом маршрута,** и записан хвостом в `PLAN.md`: сначала полный
рабочий маршрут, потом сокращение числа нажатий.

**Чем доказан.** 1104 машинные проверки (48 файлов), типы чисто, корпусный прогон не сдвинулся:
файлов 81, переписан 0, разошёлся 0; статей 37, проверено 36; блокеров 10, предупреждений 15.
Живая проверка в окне: отчёт с предупреждениями (живой урок), отчёт с блокерами (страница раздела
плагина), чистый отчёт (тестовая статья), кнопка запирается при несохранённой правке с понятной
причиной, отчёт при этом гаснет; консоль и лог сервера чистые; ни один файл статьи не тронут.

**Предыдущее закрыто.** З0 и З1а закрыты 2026-08-13; их требования остались в `TASKS.md`, история —
в `JOURNAL.md`, решения — в `DECISIONS.md`. Ничего из сделанного не отменяется и не переписывается.

**Ветка и дерево.** `main`, базовый коммит на момент записи — `5effe71`. Точка восстановления перед
переходом: ветка `backup/pre-mvp-transition` и метка `pre-mvp-transition` на `ec6cfa3`.

## 1. Что делает этот срез

**Целевой результат.** После успешной быстрой подготовки человек жмёт «Перейти к выпуску».
Программа определяет **точный состав файлов** этой статьи, показывает его, затем запускает
**полную сборку сайта** — и либо разрешает перейти к подтверждению публикации, либо показывает
понятную причину отказа. **Git при этом не меняется вовсе.**

**Путь целиком:** отчёт подготовки → состав выпуска на экране → полная сборка → «можно перейти
к подтверждению» либо ошибка сборки с её выводом.

**Входит.**
1. Правило состава выпуска в ядре: по открытой версии — все существующие языковые версии, файлы
   их состояния и картинки их папок, у каждой строки названа причина включения.
2. Ручка состава и ручка полной сборки. Сборка — дочерним процессом самим `node`, без оболочки,
   прямым путём к сборщику; предел времени — настройка.
3. Панель выпуска в окне: перечень файлов, ход сборки, итог.
4. Отказ вместо угадывания: состав, который нельзя доказать существующими правилами, называется
   разрывом, а не додумывается.

**Не входит** (отложено, не отменено): запись в git — `add`, `commit`, `push` · слежение за сборкой
на GitHub · переход от находки к полю · полировка отчёта подготовки · новые проверки шапки ·
комментарии ИИ · архив.

## 2. Ответы разведки — доказаны кодом, решения владельца не требуют

1. **Проектная команда сборки** — `npm run build`, то есть `docusaurus build` (`package.json`).
   Программа зовёт её прямым путём: `node node_modules/@docusaurus/core/bin/docusaurus.mjs build`.
2. **Языки** — все три сразу: `docusaurus build` без ключа `--locale` собирает все известные локали
   (`en`, `ru`, `es` в `docusaurus.config.js`). Отдельного перечня в программе не заводится.
3. **Связь статьи с её файлами уже есть:** `путиДругихВерсий` в ядре (правило состава статьи, у него
   до сих пор не было боевого читателя — этот срез и есть названный читатель), `statePath` для файла
   состояния, `файлСтатьи` для признака «это статья».
4. **Состав определяется без новых правил о путях.** Единственная честная дыра: если в папке версии
   лежит вторая статья, принадлежность картинок доказать нечем — тогда показывается разрыв.

## 3. Условия владельца и как они выполнены

1. **Git не трогается.** Ни `add`, ни `commit`, ни `push`, ни индекс. Доказывается тестом и сверкой
   `git status` до и после живой проверки.
2. **Посторонние файлы в состав не попадают.** Состав считается только по путям этой статьи; широкие
   операции по рабочему дереву не применяются. Доказывается целевым тестом.
3. **Перечень показывается до запуска сборки** — сначала список, потом сборка.
4. **Полная сборка — окончательная защитная проверка.** Зелёный быстрый отчёт сам по себе готовности
   не означает, и в окне это сказано словами.
5. **Сборка упала — к подтверждению не переходим**, показываем краткую причину и сохраняем доступ к
   исходному выводу сборщика.
6. **Любая правка гасит прежний результат** — тем же простым решением, что и в срезе 1: состав и
   итог сборки живут в окне и стираются при первой же правке и при переходе к другой статье.
   Постоянная машина состояний не строится.

## 3. Границы, заданные владельцем 2026-08-13

- **Полная сборка не отменена как защитная граница публикации.** В этот срез она не входит, но
  считать статью готовой к отправке без успешной полной сборки нельзя: она обязана встать перед
  публикационным коммитом. Записано в `MVP.md`.
- **Пятнадцать заданий группы 2 не отменены.** Комментарии к фрагментам и работа с внешним ИИ —
  обязательная часть полезного MVP и **следующий приоритет после первого безопасного выпуска**, а не
  полировка на неопределённый срок.
- **Необратимое удаление не развивать.** В маршруте MVP не используется; риск случайной потери
  записан хвостом в `PLAN.md`, но текущий срез из-за него не расширяется.
- **Неблокирующие замечания** идут в хвост `PLAN.md`, работа продолжается.

## 4. Как проверяется

Целевые тесты изменённого пути · один прогон существующего набора · одна живая проверка маршрута в
окне. Повторные независимые круги ворот не запускаются: круг повторяется только по затронутой
проверке и только если найден доказанный дефект этого среза.

## 5. Точка продолжения

**Минимальный разрыв до настоящего выпуска — запись в git, которой в программе нет вовсе.** Человек
уже видит точный состав и знает, что сайт собирается; дальше он всё ещё бессилен: коммита и отправки
программа не делает.

**Срез 3 — подтверждение и коммит:** кнопка подтверждения оживает, `git add` строго по перечню
состава, коммит статьи, отказ без потерь. Отправка (`push`) и слежение за сборкой на GitHub — срез 4.

**Что после первого безопасного выпуска** (решение владельца): ближайшим приоритетом становится
минимальный рабочий круг комментариев к фрагментам и работы с внешним ИИ, а не расширение проверок
и не полировка публикации.

**Указатель.** Порядок движения — `MVP.md`. Зачем программа — `BUSINESS.md`. Задания и правила —
`TASKS.md`. Почему так решено — `DECISIONS.md`. Запреты — `SPEC.md`. Живые хвосты — `PLAN.md`.
Код по файлам — `MAP.md`. Отчёт владельцу — строго по `REPORT.md`, шесть частей.
