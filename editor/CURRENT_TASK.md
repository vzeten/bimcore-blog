# Операция редактора ED-024-2: просмотры без прочерков и с обновлением раз в 6 часов, 2026-09-24

Продолжение принятой `ED-024` (карточка на доске `it/web/learn/data/editor-board.md`). Прежняя карточка — история в Git.

## Снимок

- operation: `ED-024-2`; class: `1` (показ и частота чтения внешнего источника; статьи, публикация, Git и секрет
  не меняются) → проверки исполнителя; Codex — по мере очереди.
- branch: `fix/editor-views-cache`; base: `editor` `d3a64304`; target: `editor`.
- worktree: `editor/.coordination/worktrees/fix-editor-views-cache`; `npm.cmd ci` в `editor/` — код 0.
- ведущий и исполнитель: Claude (ВК).

## Польза

Владелец (2026-09-24): при обновлении страницы вместо просмотров мелькает прочерк, хотя цифры измениться не могли;
новые данные Google он видит около полуночи, а снимок раз в сутки мог застрять на неполном дне до следующих суток.

## Результат (достаточно)

1. Окно запоминает последние показанные цифры в браузере (`localStorage`) и при открытии реестра показывает их
   сразу; свежий ответ сервера встаёт на их место. Ответ без цифр запомненные не затирает, но показывается как есть.
   Хранилище недоступно или запись испорчена — как раньше, прочерки до ответа.
2. Google спрашивается, когда удачному снимку больше `просмотры.обновлениеЧасов` (6 ч), а не раз в местные сутки:
   досчитанное за ночь подтягивается само. Пауза после неудачи (`паузаЧасов`), общий предел экземпляров и прочие
   правила `ED-024` не меняются.

## Границы

Не входят: расписание запроса к точному часу, фоновый опрос без открытого реестра, другие метрики и вид окна.

## Приёмка и следующий шаг

Владелец принимает по тестам (проверить физически не может, его слово 2026-09-24). Адресные тесты
(`viewsRemember.test.ts`, `viewsRoute.test.mjs`), типы, полный набор → перенос в `editor`, перезапуск 4780.

Состояние (24.09): типы 0, полный набор 3421/3421 (`publishAnyBranch` — известный сбой под нагрузкой, отдельно 8/8).
**Принято владельцем по тестам**, перенесено в `editor`.
