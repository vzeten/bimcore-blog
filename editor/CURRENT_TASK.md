# Операция редактора ED-024: просмотры статей из Google Analytics, 2026-09-23

Карточка `ED-024` на доске `it/web/learn/data/editor-board.md` (запись «Сейчас» 2026-09-22). Прямое поручение
владельца 22.09 через Советника («передавай пусть делает») с его ответами на открытые вопросы; явное настояние
владельца меняет приоритет. Прежние записи этого файла — история в Git.

## Снимок

- operation: `ED-024`; class: `2` (новое чтение внешнего сервиса с секретом; статьи, публикация и Git не
  меняются) + новая граница внешнего сервиса и секрета → контролёр Codex до кода и итоговый.
- branch: `feature/editor-article-views`; base: `editor` `d33ba772`; target: `editor`.
- worktree: `editor/.coordination/worktrees/feature-editor-article-views`; `npm.cmd ci` в `editor/` — код 0.
- ведущий и исполнитель: Claude (ВК); контролёр: Codex `gpt-5.6-sol`.

## Польза

Владелец видит в редакторе, какие статьи читают, и решает по этому, что писать и доводить. Сейчас для этого
нужно идти в Google Analytics, где статистика не разложена по статьям и языкам.

## Результат (достаточно)

1. Реестр: у каждой статьи сайта число просмотров страниц за последние 30 полных дней (по вчерашний день),
   сумма всех её языков. Статья вне сайта (песочница) и ещё ни разу не выходившая — прочерк, не 0.
2. Щелчок по числу открывает окно аналитики статьи: 30 дней против предыдущих 30 (оба числа и изменение);
   те же 30 дней по языкам RU/EN/ES и общая сумма; помесячно за 12 последних полных месяцев плюс текущий.
3. Источник — GA4 Data API, ресурс `531436914`, только поток learn `14901060917` (второй поток ресурса —
   магазин bimcore.one — не считается). Метрика `screenPageViews`, измерение `pagePath` + `date`.
4. Статья = все адреса всех её языков: нынешние (`адресВерсии`, та же формула, что у публикации) и прежние —
   адреса прошлых значений `slug` и прошлых путей файла из истории Git. Адреса сверяются без хвостовой косой,
   без `?…` и `#…`. Один адрес не засчитывается двум статьям; спорный адрес не засчитывается никому.
5. Запоминание: ответ Google хранится в `<материалы>/editor/.views/` (в `.gitignore`), с датой запроса.
   Новый запрос — не чаще раза в сутки и только когда нужен (открыт реестр); данные GA4 запаздывают ~сутки,
   окно подписывает, по какой день цифры. Сбой запроса оставляет прежние цифры с датой, новые не выдумываются.
6. Ключ: единственный файл `*.json` в папке из настройки (`C:/My_keys`). Программа читает его сама; содержимое
   ключа, пропуск Google и текст ответа с ними не попадают в журнал, в ответ браузеру, в тексты и в тесты.
   Ключ внутри рабочей копии Git (найден `.git` выше по пути) — отказ читать с причиной словами.
7. Нет папки, нет файла, файлов больше одного, файл не ключ, Google не принял ключ, нет доступа к ресурсу,
   нет связи — реестр и статьи работают как раньше; вместо чисел прочерк и одна понятная надпись с причиной
   и что сделать. Ни одна из этих бед не мешает открыть, сохранить, опубликовать или удалить статью.

## Границы

Не входят: другие метрики (пользователи, время, источники), отбор и сортировка реестра по просмотрам,
просмотры на сайте для читателей, статистика магазина, свой график-библиотека (столбики — обычная разметка),
запуск на сервере (переход без ключа — отдельное решение владельца). Шапки статей, публикация и Git не трогаются.

## Приёмка и следующий шаг

1. Контролёр Codex до кода по этому контракту.
2. Код, адресные тесты на поддельных ответах и поддельном ключе (подсчёт 30/30, месяцы, языки, прежние адреса,
   спорный адрес, запоминание и его срок, все беды п. 7, отказ ключа внутри Git), полный набор, типы.
3. Живая проба исполнителя на копии материалов владельца с настоящим ключом: цифры сверены с GA4 по 2–3 статьям.
4. Итоговый контролёр; проба владельца на отдельном порту; перенос в `editor` после его слова.

Условия Codex до кода (23.09, BLOCK_REAL_RISK по устройству — приняты целиком, в код):
- прежние адреса — из опубликованной ветки (`origin/main`), запоминание по её SHA, а не по `HEAD`; путь и `slug`
  одного исторического файла вместе через `адресВерсии`; переименования связываются; только отчётный период;
- суточный предел — общий для всех экземпляров: атомарная отметка попытки в `.views`, снимок — атомарной заменой;
- хранить только `pagePath/date/views`, код состояния и даты; пропуск Google — только в памяти одного захода;
- `/api/views` — отдельным запросом окна, реестр и работа со статьёй его не ждут; конечный тайм-аут; фиксированные
  адреса Google без перенаправлений; фильтр `streamId`; наружу только свои коды, исключения не в журналах;
- ключ: канонический путь, предки без `.git`, единственный обычный JSON, `service_account`; тест с секретной строкой.

Состояние: контролёр до кода пройден с условиями; код.
