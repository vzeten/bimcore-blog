# Завершённая Claude-сессия остаётся открытой

## Результат

В ветке `fix/editor-open-session-contract` от `6a93eed` процессный тест соответствует уже
утверждённому правилу: конечное состояние нынешней операции завершает ожидание и позволяет отдать
отчёт, даже если видимая Claude-сессия остаётся открытой в истории.

Класс риска: 1. Это служебное завершение принятого правила, не новая функция редактора.

## Границы этой операции

- Изменить только устаревшее утверждение в `editor/tests/processContract.test.mjs`.
- Сохранить проверки трёх конечных состояний: `ready_for_review`, `owner_required`, `failed`.
- Сохранить различие: `running` означает продолжать ждать, исчезнувший PID при `running` означает
  сбой завершения, живой PID после конечного состояния работе не препятствует.
- Не менять функциональный код, документы продукта, публикацию, статьи, песочницу и другие тесты.
- Не возобновлять закрытые процессные опыты и не добавлять новый механизм ожидания.

## Приёмка

- Адресный тест `processContract.test.mjs` проходит.
- Diff содержит только `CURRENT_TASK.md` и исправление устаревшего утверждения теста.
- Контрольный коммит, чистое дерево, короткий `handoff.md` и точный `run-status.json`.
- Итоговый контролёр и живая проба владельца для класса 1 не нужны.
