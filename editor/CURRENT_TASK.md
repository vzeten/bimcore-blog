# Вернуть обычному окну реальные рабочие материалы

## Результат
Обычное окно редактора использует принятый код, но всегда читает материалы из
`C:/My_code/bimcore-blog`, где владелец создаёт и правит статьи. Смена версии кода и перезапуск
не подменяют эту папку отдельной рабочей копией операции.

## Снимок
- card: `ED-019`; operation: `material-source`; class: `2`; state: `prepared`.
- branch: `fix/editor-material-source`.
- base: `5f0447a68b3bba38fa7f54c8d171e894d154f448`.
- target: `editor`.
- worktree: `C:/My_code/bimcore-blog/editor/.coordination/worktrees/fix-editor-material-source`.
- owner materials: `C:/My_code/bimcore-blog`.

## Подтверждённые входные данные
- Все десять последних RU-файлов найдены в основной папке; пути, размеры и SHA-256 сохранены в
  `editor/.coordination/missing-articles-evidence.json` основной папки.
- Нынешний `localhost:4780` обслуживает код из рабочей копии `fix/editor-locale-live-link` и
  читает материалы из её корня. В ней физически есть только одна из десяти статей.
- Снимок нынешнего `/api/articles` сохранён в `editor/.coordination/articles-probe-4780.json`.
Эти доказательства использовать, поиск не повторять. Проверить правила отбора и фильтры, прежде
чем окончательно назвать причину отсутствия.

## Границы
- Claude выполняет диагностику и минимальное достаточное исправление в этой ветке.
- Сначала сравнить найденные версии; статьи, медиа, черновики и незавершённые правки не менять.
- Основную папку не переключать, не очищать и не прятать; ничего не сливать целиком в `main`.
- Сохранить разделение: `main` — публикация, `editor` — принятый код, одна ветка на операцию.
- Если достаточно явной настройки или команды запуска, архитектуру хранения не менять.
- Закрепить один проверяемый способ запуска: код может быть из принятой или рабочей ветки,
  материалы всегда явно берутся из `C:/My_code/bimcore-blog`.
- Новые улучшения, миграция материалов и переустройство Git не входят.

## Приёмка и остановка
- В обычном окне видны все десять путей из снимка доказательств с их последними версиями.
- Проверено, что отсутствие не вызвано действующим фильтром или ошибочным правилом отбора.
- До и после открытия всех десяти файлов их SHA-256 и содержимое неизменны.
- После остановки и повторного запуска тем же закреплённым способом все десять снова видны.
- Адресные тесты покрывают выбор корня и отказ при неверной настройке; затем типы и полный набор
  класса 2 один раз после последней правки. Отдельный контролёр проверяет только новый diff.
- Остановиться на чистом точном коммите со статусом `ready_for_review`; merge, push и публикации нет.
