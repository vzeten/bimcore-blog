# ED-043: в панели виден только основной редактор

## Результат

После открытия панели владелец видит одну строку основного редактора на `4780`. Испытательные
порты в панели не показываются, а нынешние открытие, запуск, остановка и защита от чужого процесса
сохраняются без изменений.

## Снимок

- operation: `panel-owner-only`; class: `1`; state: `ready_for_review`.
- card: `ED-043`.
- branch: `fix/editor-panel-owner-only`.
- base: `497f5bcc8b359b72992c20ea42adb1ab5ab9f789`.
- target: `editor`.
- worktree: `C:/Users/intli/.codex/worktrees/1abc/bimcore-blog`.
- рабочие `localhost:4779` и `localhost:4780` не останавливать и не подменять во время разработки.

## Границы

- Изменить только список портов, который панель наблюдает и принимает в своих действиях.
- Панель всегда наблюдает только `4780`; значения `портыИспытаний` больше не влияют на её строки.
- Не менять опознание экземпляра, запуск, открытие, остановку и сверку владельца порта.
- Не искать и не останавливать испытательные, случайные или неизвестные процессы.
- Не менять редактор статей, материалы, черновики, публикацию и общий диспетчер серверов.
- Если для результата требуется переделка обнаружения процессов или защит ED-031, остановиться без кода.

## Проверка и остановка

- Адресный тест доказывает, что список наблюдения равен ровно `[4780]`, даже если в настройках есть
  испытательные порты.
- Существующие проверки безопасной остановки и чужого процесса проходят без изменений.
- Типы и полный набор не нужны для класса 1; выполнить адресный тест панели и один живой проход.
- Остановиться на чистом контрольном коммите со статусом `ready_for_review`; push и публикация запрещены.
