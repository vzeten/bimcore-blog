# Операция редактора ED-054 (2г): ручные SEO-записи — красным; замена картинок статьи — в «Действиях», 2026-09-24

Карточка `ED-054` на доске `it/web/learn/data/editor-board.md`. Операция 2б принята и перенесена (`editor`
`71fb2018`). Эта — по слову владельца 24.09: ручные записи (их добавляет другая сессия) реже и важнее, их нужно
видеть отдельно.

## Снимок

- operation: `ED-054-2г`; class: `2` (оформление окна над уже хранимыми данными) → проверки исполнителя,
  Codex — по мере очереди (решение владельца 2026-09-19: принятое пробой принято).
- branch: `feature/editor-manual-marks`; base: `editor` `71fb2018`; target: `editor`.
- worktree: `editor/.coordination/worktrees/feature-editor-manual-marks`; `npm.cmd ci` в `editor/` — код 0.
- ведущий и исполнитель: Claude (ВК).

## Результат (достаточно)

1. Под столбиком ручные записи — своим красным знаком с числом («▲ 2»), отдельно от серого знака изменений статей.
2. В сводке у знака ручные идут первыми; в списке периода ручные строки — первыми и красным; в «Действиях» вид
   «SEO вручную» — красным.
3. Дефект ED-054 (найден владельцем 24.09: замена картинок ретро-розетки осталась только ручной записью): разбор
   отбрасывал всё, кроме `.md`/`.mdx`. Теперь изображения рядом со статьёй (папка статьи или `img/` в ней)
   связываются со статьёй и локалью и входят в действие этой статьи (слово владельца 24.09: одна публикация
   статьи на одном языке — одно действие): «Правка · текст +3 / −1 строк · изображения +2 / −2»; только картинки —
   тоже «Правка»; у новой и удалённой статьи — внутри «Новая»/«Удалена». Служебные файлы и ничьи картинки не
   считаются. «Что изменилось» — разница текста и под ней список изображений. В базе — три новые колонки
   картинок, добавляются к старой базе без потери строк и описаний; история разбирается заново один раз в фоне.

## Границы

Не входят: хранение и схема базы, правила записи ручных событий, журнал `ED-054-2в`, воронка.

## Приёмка и следующий шаг

Типы, полный набор, проба 4798 → слово владельца → перенос в `editor`, перезапуск 4780, уборка.
