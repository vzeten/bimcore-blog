> **Передача в Claude, 2026-09-15.** Новую работу не начинать. Единый стандарт завершён локально и проверен; новый пакет не опубликован. Актуальная передача: [claude-transition-handoff.md](.coordination/claude-transition-handoff.md). Ниже — исторические записи, не поручения возобновить операции.

> **Актуально на 2026-09-15.** Ниже — история операции 2026-09-13: её SHA и состояния не возобновлять.
> Выпуск процесса 84340848 завершён; равенство с сервером проверялось при выпуске. Accepted-editor 76049023, окно 4780 читает
> материалы основной папки. Текущая работа — единый стандарт статей: карточка «Инвентаризация единого
> стандарта статей» в `it/web/learn/tasks-active.md` базы. Состояние копий — `npm.cmd run repo:check`.

# Операция процесса: настройка порядка (этап 4), 2026-09-13

## Результат

Инструкции репозитория, схемы и карточка операции приведены к одному порядку: статья, код редактора,
код сайта — три разных маршрута в одной папке; одна ведущая сессия и один пишущий на операцию;
продолжение работы восстанавливается по записи, а не по памяти чата или живому процессу.

## Снимок

- operation: `process-setup-stage4`; class: `1` (только документы); state: `done_local` (закреплено в `main` и `editor` локально; push нет).
- card: `meta/tasks-active.md` в базе, задача «Настроить спокойную работу в Claude Code и Codex» (этап 4).
- branch: `main` в основной папке `C:/My_code/bimcore-blog`, коммиты C1 `8af01006`, C1b `b2f5452e` и закрепление итоговых правок (SHA — в
  `editor/.coordination/process-completion-handoff.md`); `origin/main` остаётся `a2cfc42e`, push не делался.
- accepted-editor: `editor` C2 `4a69f8df`, C2b `2d24ffb3` и парный коммит итоговых правок, чистая; окно 4780 и панель 4779 запущены из неё. Экземпляр 4780 называет
  `1c1bf8cf`: процесс запущен 2026-09-12 14:13, раньше коммита `745037fc`; тот коммит менял только документы,
  тест, `scripts/repo-check.mjs` и хук старта, серверный код не менял. Перезапуск не нужен и не делается без слова владельца.
- пишущий: Claude (эта видимая сессия); независимая проверка: Codex.

## Активная операция по коду редактора

Нет. `ED-033` принята владельцем и перенесена в `editor`; ветка и копия убраны. `ED-044` выбрана
менеджером 2026-09-12, но операция не оформлена: ветки, копии и исполнителя нет. Открывать её —
отдельное решение ведущего с владельцем, не продолжение этой карточки.

## Что лежит незакоммиченным в основной папке

1. Документы процесса этой операции — принятый владельцем порядок. Точный пакет коммита (только он): `CLAUDE.md`, `AGENTS.md`, `editor/TASKS.md`, `editor/REPORT.md`,
   `editor/process/README.md`, `editor/process/task-manager.md`, `editor/tests/processContract.test.mjs`,
   `scripts/repo-check.mjs`, навыки `codex-gate` и `editor-change`, `output/process-review/**`. Снимок чужих файлов —
   `editor/.coordination/process-completion-before.json` (81 файл), сверяется до и после коммита.
2. Не относящиеся к этой операции изменения файлов сайта от 2026-09-13 около 19:24–19:38. Их не трогать;
   для завершения процесса решение по ним не нужно, происхождение выяснит ответственная сессия, когда
   возьмётся за них: `docusaurus.config.js`, `src/components/ProductCard.jsx`, `src/theme/SiteMetadata/index.js`,
   `content-quality-rules.json`, `scripts/seo-audit.mjs`, `plugins/translation-map/map.mjs`,
   `i18n/*/docusaurus-plugin-content-blog/options.json`, статьи `welcome` и `free-revit-family-websites`,
   все `product.*` семейств, `static/img/authors/ivan.jpg`.

## Устаревшие записи, которые нельзя принимать за состояние

- `editor/.coordination/handoff.md` — передача `ED-031` (принята 2026-09-10); не результат этой операции.
- `editor/.coordination/run-status.json` — запуск `instance-control` `ready_for_review` от 2026-09-10; завершён.
- `editor/CURRENT_TASK.md` в копии accepted-editor — прежняя `ED-033`; актуальная карточка — этот файл и
  `meta/tasks-active.md` в базе; копия обновится при следующем переносе.

## Границы

Не менялись: функциональный код, статьи, зависимости, серверы, чужие 81 файл (по снимку), база. Коммиты — только
процессный пакет в двух существующих ветках; домашний `AGENTS.md` поправлен вне Git по решению gate.
Старый `_inbox` разбирает отдельный агент. Ветка и копия для кода сайта не создавались: только порядок.

## Приёмка и следующий шаг

1. Независимая проверка Codex выполнена 2026-09-13: документы и схемы сверены, точная команда теста
   контракта (папка `editor`, с `--exclude "**/.coordination/**"`) дала 4/4.
2. Проба восстановления выполнена: помощник с чистым контекстом по `CLAUDE.md`, `AGENTS.md`, этой карточке
   и `process/README.md` назвал цель, завершённое, безопасный шаг и запреты без пересказа.
3. Очистка входящих базы завершена Codex в безопасном объёме (66 записей, снимок в `meta/archive`);
   12 сомнительных отметок и открытый остаток — отдельная дальнейшая работа, не продолжение перестройки.
4. Круг 1 (2026-09-14): `scripts/repo-check.mjs` из копии принимал копию за основную папку — исправлено (корень
   берётся у git), добавлен тест, 5/5. Отчёт — `editor/.coordination/process-completion-handoff.md`.
5. Круг 2 (2026-09-14): пакет закреплён в `main` и перенесён в `editor` патчем по точным SHA; repo:check из обеих папок,
   тест 5/5 из accepted-editor, снимок 81/81, identity 4780 прежний (процесс не перезапускался), проба маршрутов новым
   помощником пройдена. Независимый Sol проверил маршруты и дал условный GO после двух правок: домашний `AGENTS.md`
   (вне Git) и пункт 3 кода сайта (проверка по ущербу).
6. Дальше это не продолжение карточки: `ED-045` (admonitions), уточнение `ED-005` (ProductCard), одна условная EN/ES задача
   навыка — отдельные операции по действующему процессу; старые SHA прежних операций не возобновляются. Входящие базы:
   снято 248 из 579 строк, 331 иных тем остаются у назначенного помощника. Проект `My_work` в списке проектов Codex —
   один ручной шаг владельца, инструмент добавления недоступен. Push документов в `origin` — только по слову владельца. Проверка состояния уже допускает ветки
   `feature/site-…` и `fix/site-…`; реальная ветка кода сайта не создавалась.
