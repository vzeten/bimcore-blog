# Операция редактора ED-054 (2б): «Просмотры сайта» и столбики по языкам в окне аналитики, 2026-09-24

Карточка `ED-054` на доске `it/web/learn/data/editor-board.md`. Операции 1 и 2 приняты и перенесены (`editor`
`cb22cb34`). Эта — по двум словам владельца 24.09 после приёмки окна; операция 3 (воронка) — после неё.

## Снимок

- operation: `ED-054-2б`; class: `2` (окно и ручки только чтения над уже хранимыми данными) → проверки исполнителя,
  Codex — по мере очереди (решение владельца 2026-09-19: принятое пробой принято).
- branch: `feature/editor-site-views`; base: `editor` `cb22cb34`; target: `editor`.
- worktree: `editor/.coordination/worktrees/feature-editor-site-views`; `npm.cmd ci` в `editor/` — код 0.
- ведущий и исполнитель: Claude (ВК).

## Польза

Владелец видит просмотры всего сайта так же наглядно, как у одной статьи, и в поиске — какой язык растёт.

## Результат (достаточно)

1. Раздел «Просмотры сайта» в окне «Аналитика»: просмотры GA4 по сайту/RU/EN/ES/странице, шаги месяц/неделя/день,
   метки «Действий», внизу тот же список страниц, что в «Поиске Google» (просмотры 30 дней, разница в штуках,
   сортировка, щелчок — график страницы). Данные — уже хранимый снимок GA4, те же правила рядов, что у поиска.
2. Графики обоих разделов — столбики от нуля с разбивкой по языкам (переключатели раскраски, как в окне статьи),
   числа под столбиками (если помещаются, иначе по наведению), незаконченный шаг бледнее с пометкой, строка «по
   языкам за 30 дней»; по умолчанию месяцы. Показы и переходы — по-прежнему двумя графиками, без второй оси.
3. Окна графиков постоянные: «12 месяцев / 12 недель / 30 дней», недели подписаны промежутком; окно просмотров
   статьи — снова 12 месяцев (проба владельца 24.09).
4. Щелчок по столбику или ▲ выбирает период на месте: итоги и таблица страниц за него в сравнении с предыдущим
   таким же периодом, под графиками — все изменения периода, сведённые по статье с языками, кнопка «✕ К последним
   30 дням». У ▲ при наведении — только короткая сводка по видам (слово владельца 24.09).

## Границы

Не входят: воронка продаж (операция 3), новые данные и ключи, изменение хранения и публикации.

## Приёмка и следующий шаг

Тесты (части по языкам дают ровно столбик, ручки просмотров, граница шкалы), полный набор, типы → проба владельца
на 4798 → перенос в `editor` после его слова.

Состояние (24.09, ВК): пункты 1–4 готовы, полный набор 3526/3526, типы чистые; проба 4798 на копии данных владельца пройдена исполнителем, ждёт пробы владельца.
