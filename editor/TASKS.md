# Задания до конца работы

> Перечень оставшихся работ: что делать, в каком порядке, как понять, что готово.
> Составлен 2026-08-11 заново, с чистого листа: разбор вёл Fable, проверял агент-контролёр,
> второй проход Fable свёл находки. Заменяет прежний «порядок ближайших» в `PLAN.md`.
>
> **Одно задание — одна сессия.** Взял задание — сделал целиком, включая проверки и отчёт.
> Зачем нужна программа — `BUSINESS.md`, техника — `SPEC.md`, решения — `DECISIONS.md`,
> подробности текущего задания — `CURRENT_TASK.md`, живые недоделки — `PLAN.md`.
>
> Что НЕ входит в задание — не делается в нём даже «заодно». Это главная защита от разрастания.

---

## Правило проверки по риску

Сколько проверок нужно правке, решают два вопроса. Берётся строгий из двух ответов.
**«Чистая функция» и «диска не трогает» класс не понижают** — на этом уже обожглись: правило адреса
было чистой функцией, а портило файлы человека при каждом сохранении.

1. **Опирается ли на это решение о выпуске?** Правка трогает то, по чему ставится готовность,
   открывается «Опубликовать», считается состав коммита, строится адрес, имя файла или шапка при
   записи — **класс А**. Сюда же настройки и их читатели: настройка задаёт поведение. Сюда же
   вердикты, которые сами ничего не пишут, но открывают публикацию.
2. **Может ли программа после правки соврать человеку?** Правка меняет видимое — надпись, статус,
   отчёт, поле, подсказку, форму — **живая проверка в окне обязательна без исключений**: слова тоже
   поведение. Видимое, но не решающее о выпуске — **класс Б**.
3. Не решает о выпуске и видимой грани не имеет (внутренняя перестановка) — **класс В**. **Любой файл в `src/ui` — не ниже класса Б, даже при чистой перестановке:** компонентных тестов в проекте нет, а корпусный прогон смотрит статьи на диске, не окно. Машине там смотреть нечем, значит смотрят глаза.

| Класс | Порядок |
|---|---|
| **А** | карта влияния → консультация до кода → код → тесты и типы → корпусный прогон по живым статьям → живая проверка → ворота |
| **Б** | тесты и типы → живая проверка → ворота |
| **В** | тесты и типы → корпусный прогон «ничего не изменилось» (глаз заменяет он: смотреть не на что) → ворота |

**Правило остановки — вместо счёта кругов.** Круги ворот идут, пока круг приносит хотя бы одну
настоящую находку с доказательством. Блок закрывает круг без находок. Правка после ворот — новый
круг, а не хвост прошлого.

Правило проверено на трёх прошлых дефектах, и все три оно ловит: порча адреса при сохранении
(класс А по вопросу 1), `unlisted: True` — окно врало про сайт (живая проверка по вопросу 2),
надпись «Ляжет в раздел: docs/lessons» (класс Б, надписи проверяются глазами).

---

## Группа 1. Дорога к первой живой статье. Строгий порядок

### З0. Файл состояния — от файла версии, а не от папки

**Что сможет владелец.** Положить статью файлом внутрь раздела, не ломая страницу самого раздела.
**Входит.** Путь файла состояния строится от имени файла версии (`library.mjs`, `articleState.mjs`);
все читатели и писатели — реестр, сохранение, удаление, создание; перенос существующих файлов
состояния на новое правило одним проходом; закрывается хвост В1-20.
**Не входит.** Категория статьи-файла (ждёт слова владельца) · соседи при удалении (З13).
**Приёмка.** Пробная статья-файл в уроках сохранена — файл состояния страницы раздела не изменился
ни байтом, её готовность прежняя; у пробной статьи своё состояние; у всех живых статей готовность
до и после переноса совпадает.
**Класс А.** Зависит от: —. **Идёт первым:** иначе снимок, отпечатки и состав коммита строятся на
правиле, которое тут же меняется.

### З1а. Закрытие ядра подготовки

**Что сможет владелец.** В окне пока ничего; ядро становится пригодным к подключению.
**Входит.** Круг ворот по правке адресов (хвост блока Б2 — до чистого круга блок не закрыт);
ключевые слова из блокера в просьбу по слову владельца (`BUSINESS.md` 7б.11): обязательные поля рода
дают предупреждение у любой версии, `keywords: []` в файле остаётся блокером — от него падает сборка;
своя проверка «порядок в меню — число» (живой хвост, роняет сборку сайта); проверка длины описания.
**Не входит.** Сервер, окно, внешние этапы · просьба при открытии статьи (это окно, З1в).
**Приёмка.** Круг ворот без настоящих находок; статья без ключевых слов у видимой версии проходит
с предупреждением; `keywords: []` — блокер; «порядок в меню: два» — блокер с местом; документы
приведены к слову владельца в том же блоке.
**Класс А.** Зависит от: З0.

### З1б. Сервер подготовки

**Что сможет владелец.** Ничего видимого; появляется рабочая ручка.
**Входит.** Ручки старта и статуса; задача в памяти сервера, ключ — путь статьи, повторный запуск —
отказ, перезапуск сервера — честное «операции нет»; барьер: во время подготовки сохранение и
автосохранение по путям этой статьи отвечают отказом словами; снимок полного содержимого файлов
одним чтением; отпечатки файлов статьи и файлов состояния, сверка в конце; готовность ставится всем
существующим версиям только при совпавших отпечатках.
**Договор снимка.** Состав файлов берётся только из правила ядра «состав статьи». Будущий файл
комментариев (З9) расширяет одно это правило — тогда снимок, отпечатки, коммит и уборка получают его
сами, без переделки трёх заданий.
**Не входит.** Внешние этапы (З2) · окно (З1в).
**Приёмка.** Старт с отпечатком A, подмена файла на B до финала, все проверки зелёные — готовности
нет, файл состояния не тронут; сохранение во время подготовки — отказ словами, файл не изменён;
перезапуск сервера — «операции нет», готовность не поставлена; повторный старт — отказ.
**Класс А.** Зависит от: З1а.

### З1в. Окно подготовки

**Что сможет владелец.** Нажать «Подготовить», видеть ход, читать отчёт и по клику попадать к месту.
**Входит.** Кнопка и опрос хода; окно занято — переход к другой статье закрыт, автосохранение молчит;
отчёт с переходом к месту, включая переход к нужной языковой версии и раскрытие свойств на нужном
поле (сегодня свойства раскрываются только сами по себе — нужен управляемый режим); надписи находок
заводятся в настройках вместе с окном; ненавязчивая просьба заполнить ключевые слова при открытии
статьи; ответ сохранения возвращает окну исправленную шапку — закрываются два хвоста про расхождение
окна с диском; подготовка выносится своим модулем, а не доливается в файл окна на пределе размера.
**Не входит.** Внешние этапы · галочки версий · свёртка отчётов.
**Приёмка.** В любой момент подготовки на экране названы идущий этап и прошедшее время, смена этапа
видна без действий человека; клик по находке открывает нужную версию и ставит курсор в поле или
строку; **отчёт честно перечисляет этапы, которые не выполнялись** (до З2 это проверка качества и
сборка — строкой «не проверялось», а не молчанием); свежесозданная статья с заполненным описанием
проходит, хотя у заглушки нет ключевых слов, и в отчёте про это предупреждение; после сохранения
с защитной правкой поле в свойствах совпадает с файлом без переоткрытия статьи.
**Класс А + Б.** Зависит от: З1б.

### З2. Внешние этапы: проверка качества и полная сборка

**Что сможет владелец.** Зелёная подготовка означает «сборка сайта это переживёт».
**Входит.** Дочерние процессы самим `node`, без оболочки, прямым путём к сборщику; таймаут —
настройка; отбор находок по точным путям версий с отделением номера строки; «проверено 0 файлов»
при непустой статье — блокирующий сбой этапа; красная сборка по чужой вине — прямые слова и хвост
лога; внешние этапы не запускаются, если блокеры уже найдены.
**Не входит.** Разбор вывода сборщика в структурные находки (отказ записан) · починка чужих статей.
**Приёмка.** Подложный ответ «проверено 0» даёт блокер; находка по соседней статье с похожим путём
в отчёт не попадает; живая проверка идёт на пробной статье в настоящих папках сайта (песочницу
проверка качества не видит — ложный зелёный), статья убирается до отчёта; строка «не проверялось»
из отчёта исчезает.
**Класс А** — сам ничего не пишет, но его вердикт открывает публикацию. Зависит от: З1в.

### З3а. Публикация: коммит по перечню с подтверждением состава

**Что сможет владелец.** Нажать «Опубликовать», увидеть состав настоящего коммита, отказаться без потерь.
**Входит.** Слой записи git с нуля — сегодня git только читается: добавление по явному перечню путей,
коммит, снятие коммита, проверка ветки и чистоты индекса, список отличий от опубликованной ветки одним
запросом; состав статьи правилом ядра (все версии, файлы состояния, картинки; добавленные,
изменённые, удалённые, переименованные); никогда «добавить всё» и никогда «коммит со всеми правками»;
повторный прогон блокирующих проверок шапки по фактическим файлам до коммита; «Опубликовать» активна
только при готовности всех существующих версий; барьер операции на сервере и в окне; демонтаж поля
опубликованного коммита во всех четырёх местах — готовность «Опубликована» выводится из ветки.
**Не входит.** Отправка (З3б) · откат кнопкой · галочки версий.
**Приёмка.** Во временном репозитории лежат чужие изменённые и чужие новые файлы; коммит статьи
содержит ровно перечень состава и ничего сверх; отказ вернул ветку, рабочее дерево цело; ветка
разошлась с опубликованной — остановка словами до коммита; видимость сняли в файле мимо программы —
публикация останавливается и зовёт пройти подготовку.
**Класс А.** Зависит от: З2. Может занять две сессии — слой git и состав отдельно от сверок и окна.

### З3б. Публикация: отправка и слежение. **Первая живая статья**

**Что сможет владелец.** Выпустить статью на сайт из программы, при себе.
**Входит.** Отправка только явным `origin main`; повтор того же коммита без переписывания истории;
«ветка обогнала» — остановка словами; статусы «отправлено → сборка идёт → зелёная или красная со
ссылкой → на сайте»; «на сайте» — обе работы зелёные и страница отвечает; откат назван заранее.
**Не входит.** Автоповтор со счётчиком · кнопка отката · разрешение конфликтов.
**Приёмка.** Обе работы зелёные по коммиту статьи → статья открывается по своему адресу → описание на
месте, скрытая версия не торчит в меню, переключатель языков не ведёт в пустоту → главная и одна
старая статья открываются как прежде.
**Класс А.** Зависит от: З3а. Делается при владельце.

---

## Группа 2. Нужно для законченной программы

Рекомендуемый порядок: З6 → З4 → З5 → З18 → З19 → З20 → З17 → З13 → З7 → З8 → З11 → З12 → З9 → З10 → З15 → З14. З16 — в любую щель.

**Границы между соседними заданиями, где их легко спутать.** З7 делает действия над разделами
(создать, переименовать, порядок, перенос) и не делает окна настроек; З15 делает окно, из которого
эти действия вызываются, и добавляет к ним галочки полей у категорий — в него не входит ни одно
новое действие над разделами. З6 меняет только устройство знания о полях и ни одного поля не
добавляет; З15 добавляет поля категориям и ни одного правила хранения не трогает. З9 кладёт файл
комментариев в правило состава статьи и делает панель; ответы ИИ и разбор его правок — З10.

| № | Задание | Что сможет владелец | Приёмка | Класс |
|---|---|---|---|---|
| **З6** | Одно описание поля вместо восьми списков | ничего нового — и это цель | каждая живая статья открыта и сохранена без правки: ни байта изменений; список находок подготовки до и после совпадает | А, 2 сессии |
| **З4** | Подготовка сама правит механическое | нажал «Подготовить» — заголовки, подписи, призыв, обложка выправлены, список поправленного виден | перекошенная статья выправлена, правки покрашены машинным слоем, прежний текст в ленте версий, переключатель «без косметики» работает | А, 1–2 |
| **З5** | Круг после публикации | выбирает галочками, какие языковые версии выпускать; лента после публикации сворачивается | выпустил одну версию — соседняя в коммит не попала; в ленте одна отметка публикации | А, 1–2 |
| **З18** | Добавить языковую версию к статье | заводит испанскую версию у существующей статьи | версия создана скрытой, видимость от соседей не унаследована, в истории есть первый снимок с автором | А, 1 |
| **З19** | Быстрые проверки при сохранении | сразу видит, что картинка потерялась или ссылка битая | сохранил статью с битой внутренней ссылкой — сообщение появилось, текст сохранён | Б, 1 |
| **З20** | Черновик уходит при закрытии вкладки | не теряет последние секунды набора | закрыл вкладку сразу после набора — при следующем открытии текст на месте | Б, полсессии |
| **З17** | Отчёт об ошибке программы | сообщает о поломке, не собирая сведения руками | нажал в полосе ошибки — получил готовое описание с тем, что делал | Б, 1 |
| **З13** | Удаление: сосед-файл и опубликованные статьи | не теряет чужую статью при удалении; знает, что будет со старым адресом | удаление статьи, рядом с которой лежит статья-файл, её не трогает; дальше — по ответу владельца | А, 1–2 |
| **З7** | Управление разделами | создаёт раздел со статьёй-описанием, переименовывает во всех языках, меняет порядок, переносит статью | создал — сборка зелёная, раздел в меню трёх языков; переименовал — ни один язык не откатился на английский | А, 1–2 |
| **З8** | Карта видео собирается программой | не ведёт список видео руками | скрыл урок — запись ушла из карты; новый урок с видео попал сам; поля видео проверяются подготовкой | А, 1 |
| **З11** | Блоки как на сайте | видит видео, карточку товара и призыв так, как их увидит читатель | вставил блок — он показан, а не текстом разметки | Б, 1 |
| **З12** | Цвет слоёв в таблицах и быстрый поиск | видит свои правки внутри таблиц; находит статью, не открывая реестр | правка в ячейке покрашена; поиск открывает статью с клавиатуры | Б, 1 |
| **З9** | Комментарии к месту в тексте | оставляет замечание к фразе, оно живёт рядом со статьёй | закрыл и открыл статью — комментарий на месте, метка на полях против фрагмента, на сайт не попал | А, 2 |
| **З10** | Разговор с ИИ целиком | видит ответ ИИ с причиной и каждую его правку | ИИ ответил в файл — ответ виден; правка ИИ подхвачена с автором и причиной | А, 2 |
| **З15** | Окно настроек: структура и поля категорий | видит перечень доступных полей, включает их галочками; управляет структурой сайта | включил поле — новая статья категории его показывает; в живые статьи ничего не дописано | А, 2 |
| **З14** | Сотрудник и языки | отдаёт доступ сотруднику; видит плашку «перевод возможно устарел»; видит историю действий | сотрудник правит и публикует без ИИ; поправил русскую — на английской плашка | А, 2–3 |
| **З16** | Мелочи, список закрыт | форма создания называет раздел по-человечески · «Сохранить» гаснет после удаления статьи · замена картинки подключена к кнопке · списки полей уезжают в настройки | по каждому пункту — его же формулировка | Б, в любую щель |

---

## Группа 3. Быстрее и приятнее — по желанию владельца

Одна обложка вместо двух · дата календарём и метки множественным выбором · автосжатие картинок ·
выразительность формы создания (после его слов, чего именно не видно) · показ старых снимков в ленте ·
переподключение скрипта переноса из Word на ядро — только словом владельца.

## Не делать без доказанной боли или слова владельца

Единый номер захода в запросах · отдельная ручка «скрыть статью целиком» · фильтр по видимости и
таблица «язык × видимость» в реестре · разбор вывода сборщика в структурные находки · якоря находок,
переживающие правку текста · девятый список полей (вместо него З6) · массовые действия в реестре ·
автоповтор отправки со счётчиком · кнопка отката публикации · молчаливые `pull` и `rebase`.

---

## Объём

Мерка взята из факта: за 4–11 августа половина круга подготовки заняла 12–13 кругов ворот, и находки
были настоящими до последних кругов. Считаем так: блок класса А — сессия кода плюс сессия кругов
(при находках — две); блок класса Б — сессия целиком, круги в неё влезают.

- **До первой живой статьи:** группа 1, 7 блоков класса А → **14–20 сессий**.
- **До законченной программы:** группа 2, 17 заданий, из них пять двух-трёхсессионных по коду, и
  почти все класса А → **ещё 30–45 сессий**.
- **Всего 45–65 сессий.** Прежняя оценка «16 сессий на всё» была занижена вчетверо.

Цифры в таблице группы 2 — это сессии КОДА, без кругов проверки; итог их уже включает. Разброс
честный: блок без находок закрывается одним кругом, блок с находками не закрывается по счётчику.

Правило остановки делает разброс честным: блок без находок закрывается за один круг, блок с
находками не закрывается по счётчику.

---

## Ждёт слова владельца

Работа по этим пунктам не начинается, пока он не ответит; всё остальное делается без него.

1. **Шесть живых статей сегодня не прошли бы подготовку — у них нет описания.** (Ещё у трёх нет
   ключевых слов, но после вашего слова от 11 августа это уже только предупреждение.) Дозаполнять при
   первой правке или завести исключение для статей, вышедших до программы?
   *Рекомендация: без исключения — описание дописывается за минуту, исключение живёт в коде вечно.*
2. **Удаление опубликованной статьи и старые адреса.** Пока удаления нет, снять статью с глаз можно,
   открыв каждую версию и сохранив её «только по ссылке» — две-три минуты. Устраивает ли это как
   временный способ и что делать со старым адресом при настоящем удалении?
   *Рекомендация: переадресация на раздел; архивная страница — лишняя сущность.*
3. **Статья, положенная файлом внутрь раздела, считается страницей раздела** и молча теряет
   обязательные поля. Починка сдвинет категорию живой статьи «Добро пожаловать».
   *Рекомендация: чинить, «Добро пожаловать» назначить категорию явно.*
4. **Четыре пункта — его записанные обещания:** отчёт об ошибке, автоматический комментарий
   «было → стало», история действий, переподключение скрипта переноса из Word. Первые три возвращены
   в план (З17, З10, З14), четвёртое ждёт слова.
5. **Строка `BUSINESS.md` Б1 устарела:** «создание статьи в блоге пока не делается» — блог разрешён
   9 августа. Нужно его слово на правку зачёркиванием со сноской.
