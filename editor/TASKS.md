# Задания до конца работы

> Перечень оставшихся работ: что делать, в каком порядке, как понять, что готово.
> Составлен 2026-08-11 заново, с чистого листа: разбор вёл Fable, проверял агент-контролёр,
> второй проход Fable свёл находки. Заменяет прежний «порядок ближайших» в `PLAN.md`.
>
> **Одно задание — одна сессия; задание больше сессии делится на шаги** (правило — в разделе
> «Задание, шаг и сессия»). Взял — сделал целиком, включая проверки и отчёт.
> Зачем нужна программа — `BUSINESS.md`, техника — `SPEC.md`, решения — `DECISIONS.md`,
> подробности текущего задания — `CURRENT_TASK.md`, живые недоделки — `PLAN.md`.
>
> Что НЕ входит в задание — не делается в нём даже «заодно». Это главная защита от разрастания.
>
> **Порядок работы над заданием обязателен и одинаков для всех** — раздел «Как выполняется одно
> задание»: допуск, записка до кода, границы правки, что делать с находкой вне задания, точный состав
> ворот, воспроизводимая живая проверка, условия закрытия, точки обязательной остановки.
> **Критерии приёмки — обязательный минимум, а не полный список:** остальные случаи исполнитель
> выводит из инвариантов задания, и они тоже проверяются.
> Термины употребляются в значении из раздела «Словарь» в конце файла.

---

## Правило проверки по риску

Сколько проверок нужно правке, решают два вопроса. Берётся строгий из двух ответов.
**«Чистая функция» и «диска не трогает» класс не понижают** — на этом уже обожглись: правило адреса
было чистой функцией, а портило файлы человека при каждом сохранении.

1. **Опирается ли на это решение о выпуске?** Правка трогает то, по чему ставится готовность,
   открывается «Опубликовать», считается состав коммита, строится адрес, имя файла или шапка при
   записи — **класс А**. Сюда же настройки и их читатели: настройка задаёт поведение. Сюда же
   вердикты, которые сами ничего не пишут, но открывают публикацию.
2. **Может ли программа после правки соврать человеку?** Правка меняет видимое — надпись, статус,
   отчёт, поле, подсказку, форму — **живая проверка в окне обязательна без исключений**: слова тоже
   поведение. Видимое, но не решающее о выпуске — **класс Б**.
3. Не решает о выпуске и видимой грани не имеет (внутренняя перестановка) — **класс В**. **Любой файл в `src/ui` — не ниже класса Б, даже при чистой перестановке:** компонентных тестов в проекте нет, а корпусный прогон смотрит статьи на диске, не окно. Машине там смотреть нечем, значит смотрят глаза.

| Класс | Порядок |
|---|---|
| **А** | записка до кода (шаг 2), проверенная агентом-контролёром → код → предварительные тесты, типы и корпусный прогон → живая проверка → **финальный круг ворот с корпусным прогоном** |
| **Б** | записка (пп. 1, 5, 6) → код → предварительные тесты и типы → живая проверка → **финальный круг ворот** |
| **В** | код → предварительные тесты и типы → **финальный круг ворот с корпусным прогоном** |

**Живая проверка идёт там, где человеку есть что увидеть.** Шаг класса А без подключённой видимой
грани (чистое ядро, серверная ручка без окна) закрывается машинными проверками и корпусным прогоном;
живая проверка проводится в том шаге, где грань подключается, и охватывает весь сценарий целиком. Это
не поблажка: смотреть в окно там, где окно не менялось, — ритуал, а найденные владельцем дефекты
всегда жили в видимой части.

**Предварительные проверки и финальный круг — разные вещи.** Предварительные гоняются сколько угодно
раз по ходу работы, кругом не считаются и ничего не обнуляют: они нужны, чтобы находить свои ошибки
раньше проверяющего. **Круг ворот начинается после того, как код дописан и живая проверка пройдена**:
команды прогоняются заново на текущем состоянии файлов, и только этот прогон вместе с чтением
проверяющего считается кругом.

**Правило остановки — вместо счёта кругов.** Круги ворот идут, пока круг приносит хотя бы одну
настоящую находку с доказательством. Блок закрывает круг без находок. Правка после ворот — новый
круг, а не хвост прошлого.

Правило проверено на трёх прошлых дефектах, и все три оно ловит: порча адреса при сохранении
(класс А по вопросу 1), `unlisted: True` — окно врало про сайт (живая проверка по вопросу 2),
надпись «Ляжет в раздел: docs/lessons» (класс Б, надписи проверяются глазами).

---

## Как выполняется одно задание

Порядок обязателен и одинаков для всех: он написан так, чтобы две разные сессии сделали одно и то же.

### Шаг 1. Допуск к работе

Исполнитель читает своё задание целиком, `SPEC.md` по теме правки, `CURRENT_TASK.md` и хвосты
`PLAN.md`, относящиеся к заданию. **Работа не начинается вовсе**, если верно хоть одно:

- задание требует решения владельца, которого нет в разделе «Решения владельца» и нигде в `BUSINESS.md`;
- документы противоречат друг другу или документу противоречит код;
- нельзя однозначно определить, какие данные затрагиваются или что считать готовым;
- обязательная проверка задания не запускается на этой машине.

В таком случае исполнитель пишет владельцу, что именно непонятно, и ждёт. Догадка вместо ответа
запрещена.

### Шаг 2. Что записывается ДО кода

Короткая записка в ответе владельцу, до первой правки файла. **Класс А — все восемь пунктов; класс Б —
только 1, 5 и 6; класс В — ничего.**

1. какие файлы и функции меняю;
2. кто читает и кто пишет затронутые данные (все места, найденные поиском по коду);
3. какие инварианты задания обязаны остаться верными;
4. как переношу существующие данные, если правило хранения меняется, и как повторю перенос;
5. какие машинные тесты докажут поведение;
6. что проверю глазами в окне;
7. как откатиться, если правка окажется неверной;
8. какие расхождения с документами нашёл по дороге.

**Что значит «проверенная» (класс А).** Записка уходит агенту-контролёру ДО кода. Он ищет одно:
противоречия с документами, пропущенных читателей данных, нарушенные инварианты. Не нашёл — работа
идёт дальше немедленно, ответа владельца ждать не нужно. Нашёл — исполнитель либо правит записку,
либо, если решение за владельцем, останавливается по шагу 8. Классу Б проверка записки не нужна.

Расхождений нет и владелец не нужен — исполнитель продолжает сам, разрешения не ждёт.

### Шаг 3. Границы правки

- Трогаются только подсистемы, названные в задании. Соседний модуль правится, только если без него
  задание физически не работает, и это названо в записке.
- Переименования, перестановки и наведение красоты вне задания запрещены.
- Новая зависимость — только с обоснованием в `DECISIONS.md` и словом владельца (`SPEC.md` 4.10).
- Массовое переформатирование файлов запрещено: правка не должна менять строки, к делу не относящиеся.
- **Триггер объёма — проверка декомпозиции, а не авария.** Предел: 400 изменённых строк или шесть
  файлов рабочего кода (`src`, `server.mjs`), без тестов, настроек и документов. Видно из записки до
  кода, что предел будет превышен, — задание делится на шаги ДО первой правки. Предел превышен
  неожиданно во время работы — незавершённая часть откатывается или откладывается отдельным шагом,
  а не дописывается «раз уж начали»; репозиторий остаётся зелёным.

### Шаг 4. Находка вне задания

Найденное вне задания **не исправляется никогда**, даже если это одна строка. Порядок:

1. записать хвостом в `PLAN.md` с доказательством (путь, строка, чем подтверждено);
2. решить, мешает ли находка сдать текущее задание;
3. мешает — остановить задание и сказать владельцу, что оно упёрлось;
4. не мешает — продолжать, порядок будущих заданий самому не менять.

Исключение одно: находка — это порча файлов человека или падение сборки сайта. Тогда работа
останавливается немедленно и вопрос идёт владельцу.

### Шаг 5. Ворота — точный состав

«Круг ворот» означает ровно это, и заменять части нельзя:

```bash
npm run editor:test
```
```bash
npm run editor:typecheck
```

Обе команды запускаются из корня репозитория, обе обязаны завершиться без ошибок; предупреждения
допустимы, падение любого теста — нет.

Классам А и В добавляется **корпусный прогон** — он тоже команда, а не самодельный скрипт:

```bash
npm run editor:corpus
```

Прогон обходит все файлы контента (`docs`, `blog`, `i18n`), для каждого повторяет путь «программа
открыла и сохранила без правок» и сравнивает результат с содержимым файла на диске, а затем
прогоняет по ним правила подготовки и печатает сводку: сколько файлов проверено, у скольких
изменилась бы хоть одна строка, сколько находок какого уровня. Ожидание по умолчанию — ноль
изменившихся файлов; задание, которое обещает изменения, называет их заранее. **Команду заводит
первое же задание класса А (З0)**; до неё прогон считается непройденным, а не «сделанным на глаз».

Затем правку читает проверяющий: Codex (`codex-gate`), а пока он недоступен — агент
`controller`. В запрос проверяющему кладутся: задание из этого файла, изменения (`git diff`),
результаты команд, отчёт о живой проверке.

**Круг засчитан только тогда, когда проверяющий видел файлы в их текущем виде.** Круг обнуляет правка
кода, теста, настройки или документа, меняющая поведение или его описание. Дописанный отчёт, запись
доказательств и правка собственных заметок круг не обнуляют: наказывать за оформление доказательств
незачем, уверенности это не прибавляет. Блок закрывает круг без настоящих находок — то есть
без находки, подтверждённой путём к месту в коде, воспроизведением или цитатой документа. Мнение
без доказательства находкой не считается.

**Чем проверяющий круг не задерживает:** вкусом и стилем · другой архитектурой, если дефекта в нынешней
он не нашёл · пожеланиями вне задания (им место в хвостах `PLAN.md`) · повтором уже закрытого замечания
без нового доказательства · советом «сделать красивее». Это не смягчение требований, а защита от пустых
кругов: находка либо доказана, либо её нет.

### Шаг 6. Живая проверка — воспроизводимо

Живая проверка обязательна для всего, что видит человек. Чтобы разные сессии проверяли одинаково,
в отчёт идёт запись из четырёх строк:

**Кто проверяет.** По умолчанию сам исполнитель во встроенном окне на `localhost:4780`. Владелец у
экрана — смотрит он, а исполнитель проверяет следствия на диске; в отчёте прямо сказано, кто смотрел.
Среда без браузера живую проверку не отменяет: задание в ней не закрывается, а ждёт того, кто
проверит глазами. Слово «проверено» без записи ниже доказательством не считается.

- **на чём:** имя статьи и её путь (пробная статья создаётся в настоящих папках сайта, если
  задание касается публикации, иначе в песочнице `editor/sandbox`);
- **что сделал:** последовательность действий человека, по шагам;
- **что увидел:** дословно надписи и состояние на экране;
- **что на диске:** что стало с файлом после действия (или «файл не изменился»).

Пробные статьи и картинки убираются до отчёта, `git status` в конце показывает только правку
задания. Записи в `editor/.drafts` и `editor/.history` не чистятся: это данные программы.

### Шаг 7. Задание закрыто

Одновременно верно всё:

- выполнены все критерии приёмки задания;
- ворота прошли круг без настоящих находок;
- в отчёте есть доказательства: команды и их итог, имена тестов, запись живой проверки, что
  изменилось в файлах;
- рабочее дерево содержит только ожидаемые изменения;
- оставшиеся замечания названы владельцу хвостами, а не умолчаны.

### Шаг 8. Точки обязательной остановки

Здесь исполнитель останавливается и ждёт слова владельца, даже если уверен:

- **отправка на сайт** (`git push origin main`): перед ней показываются ветка, коммит, перечень
  файлов и итог ворот; отправка идёт только после отдельного «да»;
- **удаление любой статьи, которая есть в опубликованной ветке**, и любой перенос её в архив;
- **перенос живых данных**, если перенос нашёл конфликт (старое и новое состояние существуют разом);
- **смена адреса** живой статьи или категории живой статьи;
- **любая операция, затрагивающая чужие незакоммиченные файлы** в рабочем дереве;
- **вопрос, ответа на который нет в «Решениях владельца» и в `BUSINESS.md`**, и любое расхождение
  между документами, которое придётся решать выбором.

### Задание, шаг и сессия

**Задание — это возможность для владельца.** Часть заданий больше одной сессии; тогда они делятся
на шаги, и делится задание тем, кто его берёт, по правилу: **шаг оставляет репозиторий зелёным и
непротиворечивым, а недоделанную возможность — недоступной человеку** (кнопки нет, ручка не
подключена). Точка продолжения записывается в `CURRENT_TASK.md` одной строкой: что сделано, что
осталось, где остановились. Заданиям с пометкой «2 сессии» и «1–2» в таблицах шаги обязательны.

---

## Группа 1. Дорога к первой живой статье. Строгий порядок

### З0. Файл состояния — от файла версии, а не от папки

**Что сможет владелец.** Положить статью файлом внутрь раздела, не ломая страницу самого раздела.
**Правило имени, принятое здесь и обязательное.** Имя файла состояния выводится из имени файла
версии: у `index.mdx` это `_state.json` (как сегодня), у любого другого файла — `_<имя файла>.state.json`
(`a.mdx` → `_a.state.json`). Живые файлы состояния при этом остаются на своих местах и не
переименовываются: сегодня все статьи сайта лежат папками с `index.mdx`, значит **переноса данных нет
вовсе**, а правило лишь перестаёт сваливать статью-файл и страницу раздела в один файл. Имя строится
из настройки `хранение.файлСостояния`, а не пишется в коде.
**Входит.** Правило имени одним местом (`library.mjs`, `articleState.mjs`) и все его читатели и
писатели — реестр, сохранение, удаление, создание, подготовка; закрывается хвост В1-20. Здесь же
заводится команда корпусного прогона (см. ниже) — она нужна воротам этого же задания.
**Не входит.** Признак «страница раздела» и категория статьи-файла (решено владельцем, делает З16) · соседи при удалении (З13).
**Порядок внутри задания.** Сначала заводится команда корпусного прогона и запускается на текущем
коде: без этой отметки нечем доказать, что правка ничего не изменила. **Отметка обязана быть нулевой** —
ни одного изменившегося файла. Нашлись изменения ещё до правки — это чужой дефект, а не допустимая
точка отсчёта: задание останавливается, находка идёт владельцу отдельно (шаг 4). Сравнение «до было
пять изменений, после тоже пять — значит всё в порядке» правилом не считается. Только после нулевой
отметки меняется правило имени.

**Инварианты — верны для любых входов, а не только для примера приёмки.**
1. Путь файла состояния вычисляется из полного пути конкретного файла версии, а не из одной её папки.
2. Два разных файла версии не могут получить один путь состояния.
3. Правило пути живёт в одном месте; реестр, сохранение, создание, удаление и подготовка зовут его,
   а не считают сами.
4. **Переноса данных нет.** Задание не переименовывает, не перемещает, не создаёт и не удаляет ни
   одного существующего файла состояния: у всех сегодняшних статей (папка с `index.mdx`) вычисленный
   путь до правки и после правки совпадает.

**Матрица сценариев (проверяется вся).** Существующая статья папкой · статья-файл рядом с
`index.mdx` · статья без файла состояния вовсе · статья в песочнице · создание статьи-файла ·
удаление статьи-файла · одинаковые имена файлов в разных папках · имя с несколькими точками.

**Приёмка — обязательный минимум; сценарии из инвариантов исполнитель выводит сам.**
- Перечень путей всех существующих файлов состояния до правки и после совпадает целиком; ни один
  файл не создан, не удалён и не переименован.
- Готовность каждой живой статьи до и после правки одна и та же.
- Пробная статья-файл в уроках сохранена — файл состояния страницы раздела не изменился ни байтом,
  её готовность прежняя; у пробной статьи своё состояние.
- Удаление пробной статьи-файла не трогает состояние страницы раздела.
- Корпусный прогон зелёный, круг ворот без находок.

**Доказательства в отчёт.** Итог всех обязательных команд ворот · имена новых тестов · перечень путей
состояний до и после · запись живой проверки по шагу 6 · число проверенных файлов в корпусном прогоне.
**Класс А.** Зависит от: —. **Идёт первым:** иначе снимок, отпечатки и состав коммита строятся на
правиле, которое тут же меняется.

### З1а. Закрытие ядра подготовки

**Что сможет владелец.** В окне пока ничего; ядро становится пригодным к подключению.
**Входит.** Круг ворот по правке адресов (хвост блока Б2 — до чистого круга блок не закрыт);
ключевые слова из блокера в просьбу по слову владельца (`BUSINESS.md` 7б.11): обязательные поля
категории (не рода — требования задаются по категории, решение совета 2026-08-10)
дают предупреждение у любой версии, `keywords: []` в файле остаётся блокером — от него падает сборка;
своя проверка «порядок в меню — число» (живой хвост, роняет сборку сайта); проверка длины описания.
**Не входит.** Сервер, окно, внешние этапы · просьба при открытии статьи (это окно, З1в).
**Приёмка.** Круг ворот без настоящих находок; статья без ключевых слов у видимой версии проходит
с предупреждением; `keywords: []` — блокер; «порядок в меню: два» — блокер с местом; документы
приведены к слову владельца в том же блоке.
**Инварианты.** Обязательное поле категории отсутствует — предупреждение у любой версии; поле
записано пустым списком — блокер у любой версии (от него падает сборка). Описание обязательно у
каждой версии без исключений. Проверка настройки идёт раньше проверок статьи: сломана настройка —
зелёного отчёта не бывает.
**Матрица сценариев.** Все поля заполнены · нет ключевых слов у видимой версии · нет у скрытой ·
`keywords: []` · «порядок в меню» словом · описание длиннее предела · настройка сломана.
**Доказательства в отчёт.** Итог всех обязательных команд ворот · имена тестов по каждому сценарию ·
перечень мест, где документы приведены к слову владельца.
**Класс А.** Зависит от: З0.

### З1б. Сервер подготовки

**Что сможет владелец.** Ничего видимого; появляется рабочая ручка.
**Входит.** Ручки старта и статуса; задача в памяти сервера, **ключ — сама статья, а не открытая версия**
(иначе две версии одной статьи готовились бы параллельно и обе ставили бы готовность всем), повторный запуск —
отказ, перезапуск сервера — честное «операции нет»; барьер: во время подготовки сохранение и
автосохранение по путям этой статьи отвечают отказом словами; снимок полного содержимого файлов
одним чтением; отпечатки файлов статьи и файлов состояния, сверка в конце; готовность ставится всем
существующим версиям только при совпавших отпечатках.
**Договор снимка.** Состав файлов берётся только из правила ядра «состав статьи». Будущий файл
комментариев (З9) расширяет одно это правило — тогда снимок, отпечатки, коммит и уборка получают его
сами, без переделки трёх заданий.
**Не входит.** Внешние этапы (З2) · окно (З1в).
**Состояния операции — договор, обязательный к соблюдению обеими сторонами.** `нет` (не запускалась
или сервер перезапущен) · `идёт` · `завершена` · `сбой`. Для каждого задано: какие команды
принимаются, что отвечает сервер, работает ли барьер сохранения, можно ли стартовать заново, когда
снимается барьер, сохраняется ли отчёт, ставится ли готовность. Состояние живёт в памяти сервера, ключом — статья. Требование к ключу задано результатом, а не
алгоритмом: **две языковые версии одной статьи обязаны давать один ключ, две разные статьи —
разные**. Строится он существующим правилом опознания статьи (`articleKey` в ядре) после приведения
разделителей пути к одному виду. Регистр символов не меняется: правила регистронезависимых путей в
проекте нет, и заводить его здесь нельзя.

| Состояние | Старт | Сохранение и автосохранение | Ответ на запрос хода | Готовность | Отчёт |
|---|---|---|---|---|---|
| `нет` | разрешён | разрешены | «операции нет» | не трогается | пуст |
| `идёт` | отказ «уже идёт» | отказ словами, текст не теряется | этап и время | не трогается | копится |
| `завершена` | разрешён (новый прогон) | разрешены | итог с находками | ставится, **только если блокеров нет И отпечатки совпали** | хранится до следующего старта; после правки статьи не пропадает, а помечается устаревшим (инвариант 5) |
| `сбой` | разрешён | разрешены | причина и находки | не трогается | хранится так же |

**`завершена` означает только то, что операция доработала до конца, а не то, что статья готова.**
Готовность ставится при двух условиях сразу: ни одной находки уровня блокер и отпечатки в конце
совпали с начальными. Отчёт показывается всегда — и когда готовность поставлена, и когда нет.

**Перезапуск сервера возвращает состояние `нет`**, барьер исчезает вместе с памятью. Отсюда
обязательное следствие: операция, начатая прошлым сервером, не может позже изменить ни готовность,
ни файлы статьи. Дочерние процессы прекращаются вместе с сервером, а переживший его результат не
применяется вовсе: он принадлежит задаче, которой в памяти уже нет.

**Инварианты.**
1. Одновременно идёт не более одной подготовки на статью; вторая получает отказ, а не очередь.
2. Пока подготовка идёт, ни одна запись в файлы этой статьи не проходит — ни сохранение, ни
   автосохранение. Ответственность делится: **сервер (это задание)** файл не меняет и отвечает
   отказом, который окно способно отличить от прочих ошибок; **окно (З1в)** держит несохранённый
   текст у себя и повторяет запись, когда барьер снят. Текст человека не теряется ни в одном звене.
3. Готовность ставится, только если отпечатки всех файлов статьи и всех файлов состояния в конце
   совпали с теми, что сняты в начале. Не совпали — готовности нет; подготовка не записывает ни файл
   статьи, ни файл состояния и говорит человеку, какие именно отпечатки разошлись. Сам файл при этом
   мог измениться — но не её рукой, и в этом вся разница.
4. Перезапуск сервера не оставляет статью запертой: барьер живёт в памяти и исчезает вместе с ней.
5. Отчёт принадлежит снимку: он описывает те байты, которые проверялись, и не переживает изменение
   файлов молча. **Правило устаревания:** на каждый запрос отчёта сервер сверяет нынешние отпечатки
   файлов статьи со снимком; разошлись — отчёт помечен устаревшим, готовность им подтвердить нельзя,
   и окно прямо говорит, что статья изменилась после проверки. Правку могли сделать мимо программы —
   в редакторе кода, через git, другим процессом, — поэтому сброса отчёта при своём сохранении мало.
6. **Устаревший отчёт закрывает публикацию.** Своя правка возвращает версию в черновик прежним
   правилом; правку мимо программы это правило не ловит, поэтому расхождение отпечатков со снимком
   само по себе делает «Опубликовать» недоступной до новой подготовки. Иначе в коммит уехал бы текст,
   которого проверки не видели: сверка перед коммитом (З3а) смотрит только шапку, не качество и не
   сборку.

**Матрица сценариев.** Успех · находки есть, готовности нет · файл подменён во время операции ·
файл удалён во время операции · сохранение во время операции · повторный старт · перезапуск сервера
посреди операции · ошибка чтения файла · подготовка другой статьи параллельно (разрешена).

**Приёмка — обязательный минимум.** Старт с отпечатком A, подмена файла на B до финала, все проверки
зелёные — готовности нет, файл состояния не тронут; сохранение во время подготовки — отказ словами,
файл не изменён; перезапуск сервера — «операции нет», готовность не поставлена; повторный старт —
отказ; подготовка второй статьи во время первой — разрешена и не мешает.
**Доказательства в отчёт.** Итог всех обязательных команд ворот · имена тестов по каждому сценарию
матрицы · ответы сервера в каждом состоянии.
**Класс А.** Зависит от: З1а.

### З1в. Окно подготовки

**Что сможет владелец.** Нажать «Подготовить», видеть ход, читать отчёт и по клику попадать к месту.
**Входит.** Кнопка и опрос хода; окно занято — переход к другой статье закрыт, автосохранение молчит;
отчёт с переходом к месту, включая переход к нужной языковой версии и раскрытие свойств на нужном
поле (сегодня свойства раскрываются только сами по себе — нужен управляемый режим); надписи находок
заводятся в настройках вместе с окном; ненавязчивая просьба заполнить ключевые слова при открытии
статьи; ответ сохранения возвращает окну исправленную шапку — закрываются два хвоста про расхождение
окна с диском; подготовка выносится своим модулем, а не доливается в файл окна на пределе размера.
**Не входит.** Внешние этапы · галочки версий · свёртка отчётов.
**Приёмка.** В любой момент подготовки на экране названы идущий этап и прошедшее время, смена этапа
видна без действий человека; клик по находке открывает нужную версию и ставит курсор в поле или
строку; **отчёт честно перечисляет этапы, которые не выполнялись** (до З2 это проверка качества и
сборка — строкой «не проверялось», а не молчанием); свежесозданная статья с заполненным описанием
проходит, хотя у заглушки нет ключевых слов, и в отчёте про это предупреждение; после сохранения
с защитной правкой поле в свойствах совпадает с файлом без переоткрытия статьи; перезагрузка страницы
во время подготовки не теряет операцию — окно снова показывает её ход, а не пустой экран.
**Инварианты.** Окно никогда не показывает готовность, которой нет на диске. Отчёт описывает тот
снимок, по которому считался, и не переживает изменение файлов молча. Переход по находке ведёт к
существующему месту либо честно сообщает, что место исчезло.
**Матрица сценариев.** Отчёт без находок · только предупреждения · блокеры · находка без места
(красная сборка) · находка в соседней языковой версии · статья изменилась во время подготовки ·
уход из статьи во время подготовки · перезагрузка страницы во время подготовки.
**Доказательства в отчёт.** Запись живой проверки по шагу 6 · снимок экрана отчёта, а где среда его
не умеет — дословная выписка надписей с экрана · что показано в момент каждого этапа.
**Класс А + Б.** Зависит от: З1б.

### З2. Внешние этапы: проверка качества и полная сборка

**Что сможет владелец.** Зелёная подготовка означает «сборка сайта это переживёт».
**Входит.** Дочерние процессы самим `node`, без оболочки, прямым путём к сборщику; таймаут —
настройка; отбор находок по точным путям версий с отделением номера строки; «проверено 0 файлов»
при непустой статье — блокирующий сбой этапа; красная сборка по чужой вине — прямые слова и хвост
лога; внешние этапы не запускаются, если блокеры уже найдены.
**Не входит.** Разбор вывода сборщика в структурные находки (отказ записан) · починка чужих статей.
**Приёмка.** Подложный ответ «проверено 0» даёт блокер; находка по соседней статье с похожим путём
в отчёт не попадает; живая проверка идёт на пробной статье в настоящих папках сайта (песочницу
проверка качества не видит — ложный зелёный), статья убирается до отчёта; строка «не проверялось»
из отчёта исчезает.
**Инварианты.** Дочерний процесс всегда завершается: по своему концу или по таймауту, и второй
случай — находка, а не тишина. Нулевой код возврата сам по себе зелёным этапом не считается: разбор
ответа обязателен. Находки чужих статей в отчёт статьи не попадают.
**Матрица сценариев.** Всё зелено · находки проверки качества · красная сборка по этой статье ·
красная сборка по чужой статье · таймаут · процесс не запустился · «проверено 0 файлов» · статья в
песочнице (подготовка недоступна, кнопка говорит почему).
**Доказательства в отчёт.** Хвост лога сборки · время каждого этапа · запись живой проверки на
пробной статье в настоящих папках и подтверждение, что она убрана.
**Класс А** — сам ничего не пишет, но его вердикт открывает публикацию. Зависит от: З1в.

### З3а. Публикация: коммит по перечню с подтверждением состава

**Что сможет владелец.** Нажать «Опубликовать», увидеть состав настоящего коммита, отказаться без потерь.
**Входит.** Слой записи git с нуля — сегодня git только читается: добавление по явному перечню путей,
коммит, снятие коммита, проверка ветки и чистоты индекса, список отличий от опубликованной ветки одним
запросом; состав статьи правилом ядра (все версии, файлы состояния, картинки; добавленные,
изменённые, удалённые, переименованные); никогда «добавить всё» и никогда «коммит со всеми правками»;
повторный прогон блокирующих проверок шапки по фактическим файлам до коммита; «Опубликовать» активна
только при готовности всех существующих версий; барьер операции на сервере и в окне; демонтаж поля
опубликованного коммита во всех четырёх местах — готовность «Опубликована» выводится из ветки.
**Не входит.** Отправка (З3б) · откат кнопкой · галочки версий.
**Приёмка.** Во временном репозитории лежат чужие изменённые и чужие новые файлы; коммит статьи
содержит ровно перечень состава и ничего сверх; отказ вернул ветку, рабочее дерево цело; ветка
разошлась с опубликованной — остановка словами до коммита; видимость сняли в файле мимо программы —
публикация останавливается и зовёт пройти подготовку.
**Инварианты.** В коммит попадают только пути из состава статьи — ни одним файлом больше, чем бы
ни было в рабочем дереве. **Условия старта, проверяемые ДО коммита:** текущая ветка — `main`, её
вершина совпадает с `origin/main`, индекс пуст. Отстала, обогнала, разошлась или индекс не пуст —
публикация не начинается, и программа ничего не выправляет сама. Совпадение проверяется именно до
коммита: после него локальная ветка обгоняет опубликованную на один коммит, и это нормально.
Сведения об опубликованной ветке перед этой проверкой обновляются явным чтением с сервера (без
слияния: молчаливые `pull` и `rebase` запрещены); прочитать не удалось — публикация не начинается и
говорит почему, а не полагается на устаревшую запись.
**Отказ человека возвращает репозиторий к прежнему состоянию** — та же вершина, тот же индекс, то же
рабочее дерево побайтово; чужие изменения при этом не трогаются вовсе. Готовность и файлы человека
при отказе не меняются.
**Матрица сценариев.** Чистое дерево · чужие изменённые файлы · чужие новые файлы · сведения об
опубликованной ветке устарели или недоступны · ветка отстала ·
ветка обогнала · индекс не пуст · видимость правлена мимо программы · отказ человека · повторное
нажатие · сбой git посреди операции.
**Доказательства в отчёт.** Состав настоящего коммита (`git show --name-only`) · состояние ветки до
и после · итог тестов на настоящем git.
**Шаги (делить по границам модулей, а не по времени).** 1) правило состава статьи с тестами · 2) слой
записи git на временном репозитории · 3) серверная операция и барьер · 4) окно подтверждения состава ·
5) сквозной сценарий целиком. Каждый шаг оставляет репозиторий зелёным и имеет свой машинный контракт;
живая проверка — на шагах 4 и 5.
**Класс А.** Зависит от: З2. Две сессии.

### З3б. Публикация: отправка и слежение. **Первая живая статья**

**Что сможет владелец.** Выпустить статью на сайт из программы, при себе.
**Входит.** Отправка только явным `origin main`; повтор того же коммита без переписывания истории;
«ветка обогнала» — остановка словами; статусы «отправлено → сборка идёт → зелёная или красная со
ссылкой → на сайте»; «на сайте» — обе работы зелёные и страница отвечает; откат назван заранее.
**Не входит.** Автоповтор со счётчиком · кнопка отката · разрешение конфликтов.
**Приёмка.** Обе работы зелёные по коммиту статьи → статья открывается по своему адресу → описание на
месте, скрытая версия не торчит в меню, переключатель языков не ведёт в пустоту → главная и одна
старая статья открываются как прежде.
**Инварианты.** Отправка идёт только явным `origin main`. Повтор отправляет тот же коммит и не
создаёт второго. История не переписывается никогда. «На сайте» ставится только по факту: обе работы
зелёные и страница отвечает.
**Матрица сценариев.** Успех · обрыв сети · ветка обогнала · красная сборка · зелёная сборка, но
страница не отвечает · повторное нажатие · закрытие окна во время слежения.
**Доказательства в отчёт.** Ссылка на запуск сборки · адрес живой страницы · снимок состояния окна
на каждом шаге.
**Точка остановки:** перед самой отправкой — показать ветку, коммит, перечень файлов, итог ворот и
ждать отдельного «да» владельца.
**Класс А.** Зависит от: З3а. Делается при владельце.

---

## Группа 2. Нужно для законченной программы

**Правило старта, обязательное для всей группы.** Строка таблицы — не задание, а его название.
Прежде чем писать код, исполнитель разворачивает своё задание до формата группы 1: что входит и что
НЕ входит, инварианты, матрица сценариев, обязательный минимум приёмки, доказательства, точки
остановки. Лист кладётся в `CURRENT_TASK.md` и проверяется агентом-контролёром — теми же глазами, что
и записка до кода. **Код не начинается, пока лист не утверждён.** Утверждён — значит контролёр вернул проверку без
настоящих находок. Находка исправляется правкой листа, и правленый лист проверяется заново. Владелец
нужен только в случаях шага 8. Без этого правило «критерии —
минимум, остальное выводится из инвариантов» превращается в пустые слова: выводить не из чего.

**Облегчение для мелких заданий класса Б** (сегодня это З16, З20 и З11): лист сводится к трём строкам
— что входит, что НЕ входит, наблюдаемый критерий — и отдельной проверки контролёром не требует;
утверждением такого листа считается сам факт его написания в `CURRENT_TASK.md`.
Лист длиннее самой работы — это ритуал, а не защита. Задание класса А облегчения не получает никогда.

Рекомендуемый порядок: З6 → З4 → З5 → З18 → З19 → З20 → З17 → З13 → З7 → З8 → З11 → З12 → З9 → З10 → З15 → З14. З16 — в любую щель.

**Границы между соседними заданиями, где их легко спутать.** З7 делает действия над разделами
(создать, переименовать, порядок, перенос) и не делает окна настроек; З15 делает окно, из которого
эти действия вызываются, и добавляет к ним галочки полей у категорий — в него не входит ни одно
новое действие над разделами. З6 меняет только устройство знания о полях и ни одного поля не
добавляет; З15 добавляет поля категориям и ни одного правила хранения не трогает. З9 кладёт файл
комментариев в правило состава статьи и делает панель; ответы ИИ и разбор его правок — З10.

| № | Задание | Что сможет владелец | Приёмка | Класс |
|---|---|---|---|---|
| **З6** | Одно описание поля вместо восьми списков | ничего нового — и это цель | каждая живая статья открыта и сохранена без правки: ни байта изменений; список находок подготовки до и после совпадает | А, 2 сессии |
| **З4** | Подготовка сама правит механическое | нажал «Подготовить» — заголовки, подписи, призыв, обложка выправлены, список поправленного виден | перекошенная статья выправлена, правки покрашены машинным слоем, прежний текст в ленте версий, переключатель «без косметики» работает | А, 1–2 |
| **З5** | Круг после публикации | выбирает галочками, какие языковые версии выпускать; лента после публикации сворачивается | выпустил одну версию — соседняя в коммит не попала; в ленте одна отметка публикации | А, 1–2 |
| **З18** | Добавить языковую версию к статье | заводит испанскую версию у существующей статьи | версия создана скрытой, видимость от соседей не унаследована, в истории есть первый снимок с автором | А, 1 |
| **З19** | Быстрые проверки при сохранении | сразу видит, что картинка потерялась или ссылка битая | сохранил статью с битой внутренней ссылкой — сообщение появилось, текст сохранён | Б, 1 |
| **З20** | Черновик уходит при закрытии вкладки | не теряет последние секунды набора | закрыл вкладку сразу после набора — при следующем открытии текст на месте | Б, полсессии |
| **З17** | Отчёт об ошибке программы | сообщает о поломке, не собирая сведения руками | нажал в полосе ошибки — получил готовое описание с тем, что делал | Б, 1 |
| **З13** | Архив: удаление и возврат | удаляет любую статью, включая опубликованную, — она уходит в архив целиком; возвращает её оттуда обратно | удалил опубликованную — на сайте её нет после следующей публикации, в архиве целы все версии, состояния, картинки и история; вернул из архива — статья снова на месте и открывается; удаление статьи, рядом с которой лежит статья-файл, соседа не трогает; кнопки «стереть навсегда» в программе нет | А, 2 |
| **З7** | Управление разделами | создаёт раздел со статьёй-описанием, переименовывает во всех языках, меняет порядок, переносит статью в другой раздел | создал — сборка зелёная, раздел в меню трёх языков; переименовал — ни один язык не откатился на английский; перенёс — статья открывается по новому адресу, старый перестал работать и об этом сказано заранее | А, 2 |
| **З8** | Карта видео собирается программой | не ведёт список видео руками | скрыл урок — запись ушла из карты; новый урок с видео попал сам; поля видео проверяются подготовкой | А, 1 |
| **З11** | Блоки как на сайте | видит видео, карточку товара и призыв так, как их увидит читатель | вставил блок — он показан, а не текстом разметки | Б, 1 |
| **З12** | Цвет слоёв в таблицах и быстрый поиск | видит свои правки внутри таблиц; находит статью, не открывая реестр | правка в ячейке покрашена; поиск открывает статью с клавиатуры | Б, 1 |
| **З9** | Комментарии к месту в тексте | оставляет замечание к фразе, оно живёт рядом со статьёй | закрыл и открыл статью — комментарий на месте, метка на полях против фрагмента, на сайт не попал | А, 2 |
| **З10** | Разговор с ИИ целиком | видит ответ ИИ с причиной и каждую его правку | ИИ ответил в файл — ответ виден; правка ИИ подхвачена с автором и причиной | А, 2 |
| **З15** | Окно настроек: структура и поля категорий | видит перечень доступных полей, включает их галочками; управляет структурой сайта | включил поле — новая статья категории его показывает; в живые статьи ничего не дописано | А, 2 |
| **З14** | Сотрудник и языки | отдаёт доступ сотруднику; видит плашку «перевод возможно устарел»; видит историю действий | сотрудник правит и публикует без ИИ; поправил русскую — на английской плашка | А, 2–3 |
| **З16** | Мелочи, список закрыт | форма создания называет раздел по-человечески · «Сохранить» гаснет после удаления статьи · замена картинки подключена к кнопке · списки полей уезжают в настройки | по каждому пункту — его же формулировка | Б, в любую щель |

---

## Группа 3. Быстрее и приятнее — по желанию владельца

Одна обложка вместо двух · дата календарём и метки множественным выбором · автосжатие картинок ·
выразительность формы создания (после его слов, чего именно не видно) · показ старых снимков в ленте ·
переподключение скрипта переноса из Word на ядро — только словом владельца.

## Не делать без доказанной боли или слова владельца

Единый номер захода в запросах · отдельная ручка «скрыть статью целиком» · фильтр по видимости и
таблица «язык × видимость» в реестре · разбор вывода сборщика в структурные находки · якоря находок,
переживающие правку текста · девятый список полей (вместо него З6) · массовые действия в реестре ·
автоповтор отправки со счётчиком · кнопка отката публикации · молчаливые `pull` и `rebase`.

---

## Объём

Мерка взята из факта: за 4–11 августа половина круга подготовки заняла 12–13 кругов ворот, и находки
были настоящими до последних кругов. Считаем так: блок класса А — сессия кода плюс сессия кругов
(при находках — две); блок класса Б — сессия целиком, круги в неё влезают.

- **До первой живой статьи:** группа 1, 7 блоков класса А → **14–20 сессий**.
- **До законченной программы:** группа 2, 17 заданий, из них семь двух-трёхсессионных по коду, и
  почти все класса А → **ещё 30–50 сессий, разворачивание листов уже включено**. Лист пишется в
  начале первого шага задания и своей сессии обычно не требует; отдельной сессией он становится
  только тогда, когда контролёр нашёл в нём противоречия или задание пришлось делить заново.
- **Всего 44–70 сессий**, и ждать надо ближе к верхней границе: записка до кода, матрицы сценариев и
  обнуление круга при любой правке — настоящие накладные, а не бумага. Прежняя оценка «16 сессий на
  всё» была занижена вчетверо.

Цифры в таблице группы 2 — это сессии КОДА, без кругов проверки; итог их уже включает.

Правило остановки делает разброс честным: блок без находок закрывается за один круг, блок с
находками не закрывается по счётчику.

**В часах работы помощника, чтобы счёт сессий не обманывал:** сессии здесь разные — правка надписи это
час-два, серверная операция со сбоями и гонками четыре-восемь, публикация с git — до двенадцати. До
первой живой статьи ориентир **70–100 часов**, на всю программу **220–350**. Ваше собственное время
много меньше: живые проверки и решения в точках остановки, до первой публикации порядка 15–30 часов.

### Три контрольные точки вместо одного забега

1. **Сделать группу 1 целиком** — до первой живой статьи, по этому плану, без сокращения гарантий.
2. **Пожить на программе:** выпустить через неё три-пять разных материалов — новую статью, правку
   старой, скрытую языковую версию, статью с картинками. Новых возможностей в это время не добавлять,
   чинить только подтверждённые дефекты.
3. **Пересобрать группу 2 по измеренной боли, а не по списку:** что отняло больше всего ручного
   времени, где страшно нажимать кнопку, где программа сказала слишком поздно. Порядок и состав
   заданий после этого правятся вашим словом; часть из них, скорее всего, окажется не нужна в том
   виде, в каком записана сейчас.

**Замер, который делает эту точку возможной.** По каждому закрытому заданию в отчёт идут четыре числа:
сколько часов заняла работа, сколько настоящих находок дали круги, сколько раз круг обнулялся, сколько
времени съел корпусный прогон. После третьего задания станет видна настоящая скорость, и оценку выше
можно будет заменить фактом.

---

## Решения владельца 2026-08-11. Вопросов больше нет

Пять прежних вопросов закрыты его словом: по ним останавливаться больше не нужно. Точки обязательной
остановки из шага 8 при этом остаются в силе — они про другое. Полные формулировки — `BUSINESS.md`,
раздел 7в.

1. **Шесть статей без описания не трогаем.** Он допишет сам, когда откроет их. Исключения для старых
   статей не заводим: подготовка попросит заполнить, и это нормально.
2. **Удаление переносит статью в архив, и оттуда её можно вернуть** (задание З13). Временно снять
   статью с глаз — режим «только по ссылке». **Переадресации со старых адресов не создаются вовсе** —
   они уходят из плана: З7 переносит статью между разделами без них, старая ссылка перестаёт
   работать. Безвозвратного удаления нет ни у программы, ни у человека в окне.
3. **Статья-файл внутри раздела — обычная статья.** Поля, которых нет, стоят в окне пустыми; лишнее
   поле даёт предупреждение, убирает его человек сам в файле. Категорию «Добро пожаловать» назначаем
   явно (задание З16). **Инвариант, обязательный для З16 и для всякой работы с полями:** открытие
   статьи ничего не дописывает в файл — пустое поле попадает туда, только когда человек его заполнил,
   а лишнее поле программа не удаляет сама никогда.
4. **Скрипт переноса из Word не трогаем.** Возможен позже импорт из Word отдельной возможностью —
   в группу 3, без срока.
5. **Программа — полноценная CMS:** создание, правка и удаление делаются в ней целиком, включая блог.
   Строка бизнес-карты про «блог пока не создаётся» исправлена зачёркиванием со сноской.

---

## Словарь. Одно слово — один смысл

Термины ниже употребляются только в этом значении; сессия, которая поняла иначе, ошиблась.

- **Живая статья** — статья, лежащая в папках контента сайта (`docs`, `blog`, `i18n`), а не в
  песочнице `editor/sandbox`.
- **Языковая версия (существующая)** — файл статьи, лежащий на диске в корне своей локали. Версия,
  которой на диске нет, не существует; заглушка существует.
- **Видимая версия** — та, у которой в шапке нет `unlisted: true`. **Скрытая** — та, у которой оно
  есть. Скрытая существует и попадает в публикацию.
- **Готовность** — «Черновик» или «Готова к публикации» в файле состояния версии, либо
  «Опубликована», выведенная из опубликованной ветки. Принадлежит версии, а не статье.
- **Опубликованная ветка** — `origin/main` репозитория сайта.
- **Состав статьи** — правило ядра: все существующие языковые версии, их файлы состояния, картинки
  папки статьи, а после З9 — файл комментариев. Единственное место правила; снимок, отпечатки,
  коммит и уборка берут состав только у него. **Такого правила в коде пока нет — оно заводится в
  З1б;** сегодня есть только `путиДругихВерсий`, знающая одни версии.
- **Файл состояния** — служебный файл языковой версии, где живёт её готовность. Сегодня в нём есть
  ещё поле опубликованного коммита (демонтируется в З3а) и пустое место под нити комментариев.
  **Комментарии там жить не будут:** с З9 они лежат в отдельном файле комментариев статьи, и если к
  тому времени в состоянии окажутся записанные нити, З9 переносит их вместе с собой.
- **Род** — блог или документация: чем страница является для сайта и какие поля он у неё покажет.
  Задан движком, программа его не выбирает.
- **Категория** — урок, семейство, курс, справка, блог, документация, статья-описание раздела: выбор
  владельца. **Требования к полям задаются по категории, а не по роду.**
- **Статья-описание раздела** — страница, открывающая раздел меню (в коде и старых записях —
  «страница раздела», «служебная страница»). Слово «обложка» к ней не применяется: обложка — это
  картинка статьи.
- **Настоящая находка** — замечание с доказательством: путь к месту в коде, воспроизведение, цитата
  документа или красный тест. Мнение без доказательства находкой не считается.
- **Защитная правка** — исправление шапки, которое программа делает сама при сохранении, без
  действия человека, и только там, где иначе падает сборка сайта.
- **Машинный слой** — цвет, которым помечен текст, изменённый программой, а не человеком и не ИИ.
- **Лента версий** — полоса снимков статьи, свёрнутых по сеансам одного автора.
- **Корпусный прогон** — проход по всем файлам контента с проверкой «поведение до и после правки
  одинаково»; в отчёт идут число проверенных файлов и число изменившихся.
- **Работы зелёные** — обе работы GitHub Actions по коммиту публикации закончились успехом.
- **Страница отвечает по своему адресу** — запрос по адресу из шапки статьи возвращает 200 и
  содержит её заголовок.
- **Архив** — папка вне сборки сайта, куда уходит удалённая статья целиком: все языковые версии,
  файлы состояния и картинки. Из архива статью можно вернуть обратно; стереть её безвозвратно
  программа не умеет и уметь не должна. **История версий физически лежит отдельно**
  (`editor/.history`), поэтому «уходит в архив» для неё значит другое: связь истории со статьёй
  обязана пережить и архивирование, и возврат, а как это сделано — решает лист задания З13.
