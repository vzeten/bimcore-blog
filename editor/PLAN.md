# Порядок работ

> Только последовательность и состояние. Зачем — в `BUSINESS.md`, технические правила — в `SPEC.md`,
> подробности текущего задания — в `CURRENT_TASK.md`.

---

## Как идёт один круг

1. **Вход.** Замечания владельца одним письмом, в свободной форме.
2. **Разбор.** Каждое замечание получает адрес: в текущую веху, в будущую, в отложенное, либо возражение — если оно расходится с `BUSINESS.md`, спор идёт до работы, а не после.
3. **Крайние случаи.** До кода разбираются пограничные случаи задания: чего не хватает, что пусто, что противоречит. Решения записываются в `BUSINESS.md`, а не остаются в голове.
4. **Работа.** Делается только то, что относится к текущему заданию. Остальное записывается, но не делается.
5. **Проверка.** Машинные тесты, строгая проверка типов, сверка с `SPEC.md`.
6. **Отчёт.** Что сделано, что отложено и почему, сколько осталось до конца вехи.

Статусы задания: **не написано** → **написано** → **проверено машиной** → **принято владельцем**.
Веха закрыта, когда все её задания приняты владельцем.

---

## Вехи

| Веха | Что владелец сможет делать | Состояние |
|---|---|---|
| **В1. Правка** | Открыть статью, читать её как статью, править, видеть отличия от сайта и историю версий | в работе |
| **В2. Разговор с ИИ** | Оставить замечание к месту в тексте, отдать статью в Claude Code, увидеть каждую правку ИИ с причиной | не начата |
| **В3. Публикация** | Подготовить статью, получить отчёт проверки, опубликовать и убедиться, что она на сайте | не начата |
| **В4. Языки и сотрудник** | Вести русскую, английскую и испанскую версии без кальки, отдать доступ сотруднику | не начата |

---

## Веха 1 — правка. Декомпозиция

| № | Задание | Статус | Зависит от |
|---|---|---|---|
| В1-00 | Каркас окна: зоны, всплывающие команды двумя шагами, выключатель цвета | проверено машиной | — |
| В1-01 | Открытие статьи из панели, поиск по названию | проверено машиной | — |
| В1-02 | Показ статьи как статьи: заголовки, жирное, ссылки, списки | проверено машиной | В1-01 |
| В1-03 | Изображения видны в тексте | проверено машиной | В1-02 |
| В1-04 | Свойства статьи: правка шапки, поля без YAML-синтаксиса | проверено машиной | В1-01 |
| В1-05 | Таблицы показываются таблицами | проверено машиной | В1-02 |
| В1-06 | Команды правки: заголовки, списки, ссылка, жирный, таблица, вставка блоков | проверено машиной | В1-02 |
| В1-07 | Вставка изображения из буфера и замена файла | проверено машиной | В1-03 |
| В1-08 | Сохранение не портит файл | проверено машиной | В1-01 |
| В1-09 | Отмена и возврат правок | проверено машиной | В1-02 |
| В1-10 | Слои цвета: на сайте, мои прошлые правки, текущие | проверено машиной | В1-08 |
| В1-11 | Автосохранение и версии при сохранении | проверено машиной | В1-08 |
| В1-12 | Версия при изменении файла снаружи, определение автора | не написано | В1-11 |
| В1-13 | Наполнение ленты версий: сеансы, просмотр содержимого версии | не написано | В1-11 |
| В1-14 | Возврат к выбранной версии | не написано | В1-13 |
| В1-15 | Слой правок ИИ отдельным цветом по автору | не написано | В1-12 |
| В1-16 | Видео, карточка товара и призыв показываются как на сайте | не написано | В1-02 |
| В1-17 | Цвет слоёв виден внутри таблицы | не написано | В1-05, В1-10 |
| В1-18 | Отчёт об ошибке программы | не написано | — |
| В1-19 | Статья как одна сущность: категория, версии по языкам, переключатель версий в шапке | проверено машиной | — |
| В1-24 | Готовность и видимость статьи как отдельные признаки | частично | В1-20 |
| В1-25 | Проверка совпадения адресов у языковых версий | не написано | В1-19 |
| В1-20 | Хранилище: состояние версии рядом со статьёй | проверено машиной | В1-19 |
| В1-21 | Реестр статей: дерево разделов, таблица, сортировка, отбор, переключение видимости | проверено машиной | В1-19, В1-20 |
| В1-22 | Быстрый поиск поверх реестра | не написано | В1-21 |
| В1-23 | Возврат защитных правил прежней CMS: пустое `image`, правила `slug` | проверено машиной | В1-04 |

**В1-11 прошёл ворота Codex 2026-08-04** после девяти кругов правок. Основные найденные и закрытые дыры: сохранение молча затирало внешнюю правку (теперь 409 и выбор), обход проверки без отпечатка (теперь 400), клиент не подхватывал новый отпечаток и база сравнения (ложный конфликт и ложное «Сохранено»), правка во время запроса терялась, сбой сохранения не возвращал работу в черновик, поздний запрос автосохранения воскрешал черновик после сохранения (теперь запросы прерываются, а черновик, равный файлу, не пишется).

**Порядок ближайших:** В1-12 → В1-13 → В1-14 → В1-15. Задания В1-16, В1-17, В1-18 и В1-22 берём последними в вехе.

**Круг правок владельца от 2026-07-31 (после /model).** Разобран отдельным заходом. Что сделано в текущей В1: русские названия разделов в дереве и категория «страница раздела» для служебных index-страниц (В1-21); поля шапки без YAML-синтаксиса, короткий адрес (В1-04); позиционирование всплывающего меню (В1-00/06); цвет ссылок разведён со слоем правок (В1-02/10); готовность опубликованных статей больше не «Черновик» (В1-20); видимость убрана из реестра, переключается внутри статьи (В1-24, частично — смена готовности вручную остаётся в В3); подпись «Правил» → «Кто менял»; кнопка «назад» браузера возвращает в реестр; аудит хардкода (ссылки-заглушки, подсказки языков, призыв — в настройки). Признано будущим: создание статей и локалей — веха 4; создание служебных страниц разделов — веха 3 (управление разделами); редактирование существующих служебных страниц работает уже сейчас (они статьи).

**В1-24 частично:** видимость переключается внутри открытой статьи и пишется в `unlisted` всех языковых версий; готовность показывается реальной (из состояния или факта публикации). Не сделано: ручная смена готовности в интерфейсе — это часть подготовки к публикации, веха 3.

**Хвост видимости (честно):** переключение «в меню / только по ссылке» меняет файл сайта (`unlisted`), но программа пока НЕ доводит это изменение до публикации и не показывает, что появилось неопубликованное изменение. Кнопки «Подготовить» и «Опубликовать» всегда неактивны — публикация целиком в вехе 3. То есть В1 умеет менять видимость, но не умеет её опубликовать. Ложного ощущения готовности не создаём: это записано здесь как незакрытый хвост до вехи 3.

**Круг проверки Codex от 2026-07-31 (после /model).** Дополнительно сделано: баг позиционирования клика в редакторе — картинка грузилась асинхронно, CodeMirror держал заниженную высоту строки, и клик по строкам ниже попадал мимо; лечится пересчётом координат после загрузки картинки (В1-03). Правка подписи картинки: клик по картинке ставит курсор на её строку и раскрывает исходный markdown — alt правится в тексте (В1-03). Человеческие подписи полей шапки из настроек, техническое имя в подсказке (В1-04). Заголовок вставляемой таблицы вынесен в настройки (В1-06). styles.css разбит (styles + registry), тест лимита файлов расширен на CSS.

**Обработка ошибок (фундамент В1-18, от 2026-08-03) — кандидат на проверку Codex.** Минимальная защита сохранения: все запросы UI через `requestJson` (проверяет ответ, берёт текст из `{error}`); интерфейс не считает действие успешным без подтверждения сервера (провал сохранения не гасит «несохранено», сбой видимости не меняет состояние, `pasteImage` не вставляет `![](undefined)`); спокойная панель ошибки вместо `alert` с причиной от сервера; сервер разделяет ожидаемые (400/404/500) и внутренние ошибки, `/api/visibility` отклоняет плохой путь (400/404), битый JSON и тело сверх предела не роняют процесс. Стандарт — раздел «6. Обработка ошибок» в SPEC.md. Статус уточняется после повторной проверки Codex. Полный «Отчёт об ошибке» — по-прежнему В1-18.

**Блок обработки ошибок прошёл ворота Codex 2026-08-03** (4 из 5 замечаний закрыты полностью, 5-е выведено в хвост; отдельно найден и исправлен путь неправильного типа в `/api/visibility` → 400 вместо 500). Вердикт: можно стартовать В1-11.

**Хвост от ворот:** пользовательская замена картинки (`replaceImage`) к кнопке в интерфейсе не подключена — это отдельный хвост **В1-07**, к автосохранению отношения не имеет. В коде помечен `TODO(В1-07)`.

**Не засчитано (честно):**
- **В1-16** (видео, карточка товара, призыв показываются как на сайте) — НЕ сделано: блоки только вставляются как текст, нормальным видом в редакторе не отображаются. Остаётся «не написано».
- **В1-25** (совпадение адресов у языковых версий) — правило есть, но в сохранение не встроено (см. DECISIONS.md). В1-23 закрывает всё, кроме него.
- Ручная смена готовности, публикация видимости — веха 3.

Настройки черновых снимков (папка, срок, число) заведёт В1-11 — вместе с кодом, который их читает. Пока такого кода нет, этих ключей в настройках нет: настройка, которую никто не читает, обманывает. То же с цветами по авторам — они появятся в В1-15.

**Кто пишет код:** Claude. Codex в папку `editor/` не пишет.

---

## В1-11 — «Автосохранение и версии при сохранении» (спецификация владельца от 2026-08-01, НЕ начато)

> Зафиксировано по прямому заданию владельца. Не начинать, пока не сделаны предшественники и не решён порядок (версии/комментарии). Технический хвост по картинке (`load` до `src` + ветка `img.complete`) уже закрыт в `inline.ts` — переделывать не нужно.

**Зачем.** Если человек закрыл программу, обновил страницу или ушёл из статьи, не нажав «Сохранить», правки не должны пропадать. При следующем открытии он продолжает с того же места.

**Поведение.**
1. При изменении текста или свойств программа пишет рабочий черновик в служебное хранилище редактора вне git.
2. Автосохранение НЕ меняет настоящий `.mdx`.
3. Настоящий `.mdx` меняется только по кнопке «Сохранить».
4. При повторном открытии статьи программа автоматически открывает последний автосохранённый черновик, если он новее файла.
5. Окна «восстановить черновик?» в обычном случае нет — человек просто продолжает работу.
6. Если настоящий файл на диске изменился ПОСЛЕ автосохранения — показать выбор: «Открыть автосохранение» · «Открыть файл» · «Сравнить». Молча файл не подменять.
7. После «Сохранить» автосохранённый черновик очищается или переносится в историю версий.
8. Черновики не попадают в git и не лежат в папках контента Docusaurus.
9. Настройки хранения (папка, интервал автосохранения, срок жизни, максимум черновиков/снимков) заводятся ВМЕСТЕ с кодом, который их читает.

**Статусы в интерфейсе (привычные формулировки владельца).** «Сохранено» · «Сохраняем…» · «Автосохранено» · «Есть несохранённые изменения» · «Не удалось автосохранить» · «Файл изменён снаружи». Кнопка «Сохранить» = запись в настоящий файл статьи; статус рядом с кнопкой или в шапке показывает состояние автосохранения. (Статусы — в настройки, как все надписи.)

**Обязательные тесты.** Писать тестами по API/хранилищу (проверять результат запроса и содержимое хранилища), а не наличие строк в коде.

Приоритет — эти четыре пишутся первыми (test-first, до кода поведения):
- автосохранение пишет черновик вне git;
- `.mdx` не меняется без кнопки «Сохранить»;
- повторное открытие берёт автосохранение;
- конфликт с внешней правкой не решается молча (программа отдаёт выбор, не подменяет файл).

Полный список:
1. Черновик создаётся при изменении текста.
2. Черновик создаётся при изменении свойств статьи.
3. Настоящий `.mdx` не меняется без кнопки «Сохранить».
4. При повторном открытии берётся свежий автосохранённый черновик.
5. Файл на диске новее автосохранения → программа показывает выбор и не подменяет файл молча.
6. После «Сохранить» автосохранение очищается или переносится в историю.
7. Черновики не попадают в git.
8. `npm run editor:test` проходит.
9. `npm run editor:typecheck` проходит.

**Связь с существующим.** Модуль снимков `history.mjs` (сеансы, имена, разбор) уже написан в В1-20 — В1-11 подключает его к сохранению и добавляет слой автосохранения поверх. Определение автора внешней правки — В1-12, лента и просмотр версий — В1-13, возврат — В1-14.

---

## Веха 2 — разговор с ИИ

Комментарий по кнопке «Комментировать» к выделенному слову, фразе, абзацу · нить: замечание, ответ ИИ с причиной, отдельно удалённое · связь нити с текстом пунктиром, заливкой и линией, без цвета шрифта · автоматический комментарий «было → стало» на каждую правку · запись замечаний в атрибуты, видимые Claude Code · готовое задание для чата · подхват правок ИИ с диска с автором и причиной · лента версий сеансами, свёртка после публикации · цвет правок по автору из настроек.

## Веха 3 — публикация

Кнопка «Подготовить» с выбором версий галочками · порядок подготовки: проверка, затем исправление механического, затем отчёт · каждая строка отчёта ведёт к месту в статье · статус «готова к публикации» ставит программа · кнопка «Опубликовать» неактивна без подготовки · правка после подготовки возвращает статью в черновик · повтор при ошибке на стороне GitHub, отчёт при ошибке на нашей · перевод ручных мелочей публикации в машинную часть · переключение видимости «в меню» и «только по ссылке» · управление разделами сайта: переименование, порядок, создание с заглушкой, перенос статьи с переадресацией, названия разделов во всех трёх языках · создание служебных страниц разделов (их правка работает уже в вехе 1) · удаление статьи отдельным действием с предупреждением.

## Веха 4 — языки и сотрудник

Создание статьи в любой локали с попаданием в правильное место сайта · английская заглушка для статьи, живущей пока на одном языке · плашка «перевод возможно устарел» · вход сотрудника, работа без ИИ · история действий: кто, что, когда.

Сюда относится вопрос владельца «как создать статью на EN/RU/ES и добавить локаль»: в вехе 1 только просмотр и правка существующих версий, создание — здесь. В интерфейсе кнопок создания нет, ложного ощущения готовой функции не создаётся.

---

## Отложено осознанно

Разрешение конфликтов с удалённым репозиторием · окно настроек поверх `settings.json` · сжатие картинок · дописывание строки в реестр статей базы знаний · одновременная работа владельца и сотрудника над одной статьёй · массовые действия в реестре — владелец про них не говорил, а галочки съедают ширину таблицы.
