# Порядок работ

> Только последовательность и состояние. Зачем — в `BUSINESS.md`, технические правила — в `SPEC.md`,
> подробности текущего задания — в `CURRENT_TASK.md`. Рассказы о кругах ворот по прошлым заданиям —
> в `JOURNAL.md` (на старте не читается). Незакрытые недоделки — здесь, в разделе «Хвосты».

---

## Как идёт один круг

1. **Вход.** Замечания владельца одним письмом, в свободной форме.
2. **Разбор.** Каждое замечание получает адрес: в текущую веху, в будущую, в отложенное, либо возражение — если оно расходится с `BUSINESS.md`, спор идёт до работы, а не после.
3. **Крайние случаи.** До кода разбираются пограничные случаи задания: чего не хватает, что пусто, что противоречит. Решения записываются в `BUSINESS.md`, а не остаются в голове.
4. **Работа.** Делается только то, что относится к текущему заданию. Остальное записывается, но не делается.
5. **Проверка.** Машинные тесты, строгая проверка типов, сверка с `SPEC.md`.
6. **Отчёт.** Что сделано, что отложено и почему, сколько осталось до конца вехи.

Статусы задания: **не написано** → **написано** → **проверено машиной** → **принято владельцем**.
Веха закрыта, когда все её задания приняты владельцем.

---

## Вехи

| Веха | Что владелец сможет делать | Состояние |
|---|---|---|
| **В1. Правка** | Открыть статью, читать её как статью, править, видеть отличия от сайта и историю версий | почти закрыта, хвост из четырёх заданий |
| **В3. Жизненный цикл статьи** | Создать статью, довести до публикации, выпустить на сайт, управлять разделами | **следующая** |
| **В2. Разговор с ИИ** | Оставить замечание к месту в тексте, отдать статью в Claude Code, увидеть каждую правку ИИ с причиной | не начата |
| **В4. Языки и сотрудник** | Вести русскую, английскую и испанскую версии без кальки, отдать доступ сотруднику | не начата |

**Очередь вех изменена 2026-08-07 (решение владельца).** Веха 3 идёт раньше вехи 2. Причина: создание статьи руками — базовый и самый частый способ работы владельца, статьи от ИИ дополняют его, а не заменяют. Бизнес-карта ставит «написание русской статьи с нуля» первым сценарием (Б1), а план держал создание в последней вехе — это расхождение и устраняется. Содержимое вех при этом не менялось: веха 1 не ломается, её хвост (В1-16, В1-17, В1-18, В1-22) берётся после того, как заработает жизненный цикл.

---

## Веха 1 — правка. Декомпозиция

| № | Задание | Статус | Зависит от |
|---|---|---|---|
| В1-00 | Каркас окна: зоны, всплывающие команды двумя шагами, выключатель цвета | проверено машиной | — |
| В1-01 | Открытие статьи из панели, поиск по названию | проверено машиной | — |
| В1-02 | Показ статьи как статьи: заголовки, жирное, ссылки, списки | проверено машиной | В1-01 |
| В1-03 | Изображения видны в тексте | проверено машиной | В1-02 |
| В1-04 | Свойства статьи: правка шапки, поля без YAML-синтаксиса | проверено машиной | В1-01 |
| В1-05 | Таблицы показываются таблицами | проверено машиной | В1-02 |
| В1-06 | Команды правки: заголовки, списки, ссылка, жирный, таблица, вставка блоков | проверено машиной | В1-02 |
| В1-07 | Вставка изображения из буфера и замена файла | проверено машиной | В1-03 |
| В1-08 | Сохранение не портит файл | проверено машиной | В1-01 |
| В1-09 | Отмена и возврат правок | проверено машиной | В1-02 |
| В1-10 | Слои цвета: на сайте, мои прошлые правки, текущие | проверено машиной | В1-08 |
| В1-11 | Автосохранение и версии при сохранении | проверено машиной | В1-08 |
| В1-12 | Версия при изменении файла снаружи, определение автора | принято владельцем | В1-11 |
| В1-13 | Наполнение ленты версий: сеансы, просмотр содержимого версии | проверено машиной, 2026-08-05 | В1-11 |
| В1-14 | Возврат к выбранной версии | принято владельцем, 2026-08-06 | В1-13 |
| В1-15 | Слой правок ИИ отдельным цветом по автору | проверено машиной, 2026-08-06 | В1-12 |
| В1-16 | Видео, карточка товара и призыв показываются как на сайте | не написано | В1-02 |
| В1-17 | Цвет слоёв виден внутри таблицы | не написано | В1-05, В1-10 |
| В1-18 | Отчёт об ошибке программы | не написано | — |
| В1-19 | Статья как одна сущность: категория, версии по языкам, переключатель версий в шапке | проверено машиной | — |
| В1-24 | Готовность и видимость как отдельные признаки языковой версии | частично | В1-20 |
| В1-25 | Проверка совпадения адресов у языковых версий | не написано | В1-19 |
| В1-20 | Хранилище: состояние версии рядом со статьёй | проверено машиной | В1-19 |
| В1-21 | Реестр статей: дерево разделов, таблица, сортировка, отбор, ~~переключение видимости~~ показ видимости по языковым версиям | проверено машиной | В1-19, В1-20 |
| В1-22 | Быстрый поиск поверх реестра | не написано | В1-21 |
| В1-23 | Возврат защитных правил прежней CMS: пустое `image`, правила `slug` | проверено машиной | В1-04 |

**Порядок ближайших:** В1-16, В1-17, В1-18, В1-22 — остаток вехи 1.

**Кто пишет код:** Claude. Codex в папку `editor/` не пишет.

Что нашли и закрыли круги ворот по каждому заданию — `JOURNAL.md`. Настройки заводятся вместе с кодом, который их читает (SPEC 4.4.2): настройка, которую никто не читает, обманывает.

---

## Хвосты (живые)

Осознанные недоделки, показанные владельцу. Полные истории — в `JOURNAL.md`. Закрытый хвост отсюда вычёркивается; хвост старше двух вех — либо становится заданием, либо снимается решением владельца.

- **В1-07:** замена картинки (`replaceImage`) к кнопке не подключена (`TODO(В1-07)` в коде); вставка, начатая до ухода из статьи, пропадает — загруженный файл остаётся на диске без ссылки.
- **Автосохранение при закрытии вкладки:** закрытие раньше таймера теряет последние секунды набора; все уходы внутри программы правку дописывают. Лечится отправкой при закрытии — отдельная задача (записана в `IDEAS.md`).
- **Снимки прежней схемы имён:** старые папки лежат неиспользованными; тексты есть в файлах и в git, лента их не показывает.
- **Сбой на служебном шаге записи:** файл статьи пишется первым; сбой диска после него оставляет историю или готовность неполными, человеку прямо сообщается.
- **`_state.json` одно на папку статьи:** верно, пока строение `<slug>/index.mdx`; появись папка с двумя статьями — готовность одной подменит другую (правило В1-20).
- **В1-15:** теста на число прочитанных с диска снимков нет — подменить чтение нечем, новая зависимость запрещена (SPEC 4.10); потолок закреплён в коде.
- ~~**Видимость, частичная запись:** сбой на середине оставляет часть переключённой~~ — снято 2026-08-10 кругом 1б вместе с самим механизмом: сохранение пишет один открытый файл, делить на части нечего.
- ~~**Видимость без публикации:** изменение не показывается как неопубликованное~~ — закрыто 2026-08-10 кругом 1: рядом с видимостью стоит «изменение ещё не на сайте», пока она расходится с опубликованной веткой (+круг 1б: считается по открытой языковой версии).
- **Видимость соседней версии показывается только по её открытии:** статус «изменение ещё не на сайте» относится к открытой версии, и не уехавшая английская его не зажигает. Плата названа сознательно кругом 1б; публикация едет целиком, поэтому расхождение живёт недолго.
- **`путиДругихВерсий` без боевого вызова:** правило состава статьи оставлено кругом 1б с названными читателями — проверка совпадения адресов языковых версий (круг 2) и состав коммита публикации (круг 3). Тесты правила на месте.
- **Первая версия английской заглушки без автора:** создание статьи пишет снимок только своей версии, поэтому у заглушки следа нет, и первое же обращение программы записывает её как чужую правку с автором «Неизвестный». Поведение было и до круга 1 (так же вела себя ручка видимости), правило «догадок об авторе не строим» соблюдено. Лечится вместе с созданием (В3-01), а не в кругах публикации.
- **В1-25:** правило совпадения адресов языковых версий есть, но в сохранение не встроено. В1-23 закрывает всё, кроме него. Первый настоящий вызов правило получает в подготовке статьи (блок Б круга 2).
- **Окно после сохранения показывает своё, а не то, что легло на диск:** сервер правит шапку защитной проверкой, но исправленную шапку окну не возвращает, и до повторного открытия статьи поле в свойствах может расходиться с файлом. Найдено воротами блока А живой проверкой (поле из одних запятых). Блок Б убрал главный вред — пустая строка теперь уходит из файла, а не превращается в `tags: []`, — но само расхождение живёт до перечитывания статьи. Закрывается там же, где правится ответ сохранения: блок В круга 2 (барьер записи и отпечатки).
- **Форма создания статьи называет раздел техническим путём:** человек выбирает «Уроки», а строка под полями отвечает «Ляжет в раздел: docs/lessons» (`src/ui/zones/NewArticle.tsx`). Человеческое имя уже лежит в настройках и уже показано в самом списке — подставляется идентификатор узла вместо подписи. Найдено воротами блока А; правка на одну строку, но это соседнее задание, и в круг 2 её не тащим.
- **В1-16:** блоки видео, карточки товара и призыва только вставляются текстом, как на сайте не показываются — статус «не написано» честный.
- **Удаление статьи:** при сбое посреди стирания окно оставляет статью открытой, хотя её файл мог быть уже стёрт. Какая именно версия исчезла, окно не знает; человека ведёт в список статей текст ошибки вместе с перечнем стёртого.
- **В3-01Д, правка А:** запрет правки поля «Обложка» во время загрузки машинным тестом не покрыт — компонентных тестов в проекте нет, а новая зависимость запрещена (SPEC 4.10). Держится на живой проверке в окне.
- **В3-01Д, правка А:** превью в просмотре старой версии не показывается намеренно — имя обложки постоянное, и на диске давно другой файл; показ выдал бы сегодняшнюю картинку за картинку той версии.
- ~~**В3-01Д, правка Б:** форма создания статьи открыта на весь экран, раздел выбрать негде~~ — закрыто 2026-08-09: раздел выбирается в самой форме, языки ограничены разделом и его наличием в локали, причина неактивной кнопки названа словами.

---

## Веха 2 — разговор с ИИ

Комментарий по кнопке «Комментировать» к выделенному слову, фразе, абзацу · нить: замечание, ответ ИИ с причиной, отдельно удалённое · связь нити с текстом пунктиром, заливкой и линией, без цвета шрифта · автоматический комментарий «было → стало» на каждую правку · запись замечаний в атрибуты, видимые Claude Code · готовое задание для чата · подхват правок ИИ с диска с автором и причиной · лента версий сеансами, свёртка после публикации · цвет правок по автору из настроек.

## Веха 3 — жизненный цикл статьи

Порядок заданий и границы приняты 2026-08-07 после двух консультаций Codex. Владелец ушёл и передал решения по неясным местам Claude с условием обосновать каждое; обоснования и решения по границам В3-01 — в `JOURNAL.md`. Часть границ владелец снял 2026-08-09: блог и загрузка обложки разрешены, текущие правила — в `CURRENT_TASK.md` (В3-01Д).

| № | Задание | Что даёт | Состояние |
|---|---|---|---|
| В3-01 | Создание русского черновика в существующем разделе, с английской заглушкой | владелец начинает писать статьи в программе | проверено машиной, 2026-08-07; доработка В3-01Д: правка А (обложка) проверена машиной, живой проверкой и воротами 2026-08-09, ждёт приёмки владельца; правка Б (тупик формы создания) не написана |
| В3-02 | Добавление языковой версии к существующей статье | вторая половина Б9 | не написано |
| В3-03 | Подготовка и публикация | убирает ручную работу с git | не написано |
| В3-04 | Управление разделами: создать с заглушкой, переименовать, перенести статью с переадресацией | Б15 | не написано |
| В3-05 | Удаление и архив | после решения владельца по старым адресам | частично: удаление статьи, которой нет в опубликованной ветке, сделано 2026-08-09 (решение владельца). Опубликованное с переадресацией и архив — не написаны |

**«Номер захода» отменён советом 2026-08-10 и владельцем в договорённости о порядке работ.** Задание было записано 2026-08-09 как обязательное между В3-01Д и В3-03: единый номер открытия статьи в `requestJson`, ответ сервера с чужим номером молча выбрасывается. Вместо него на время подготовки и публикации окно занято, переход к другой статье закрыт, ход операции виден; сверка списка файлов с составом коммита закрывает ту же дыру там, где она опасна. Обоснование отказа — в `DECISIONS.md`, раздел «"Номер захода" отложен: на время публикации окно занято». Вернуться к заданию можно только под доказанную боль.

**Переработка переключателя видимости — сделана 2026-08-10, круги 1 и 1б задания.** Видимость стала обычным полем свойств статьи и уезжает на диск вместе с «Сохранить»; отдельная ручка `/api/visibility` удалена целиком. **Поправка владельца в тот же день, круг 1б сделан:** синхронизация языковых версий отменена, видимость принадлежит версии, сохранение пишет один открытый файл. Подробности и поведение — в `DECISIONS.md`, раздел «Круг 1б сделан 2026-08-10: сохранение пишет один файл» (он же главнее прежнего раздела «Круг 1 сделан», где отменённые куски зачёркнуты). Ниже — исходные замечания владельца 2026-08-06, ради которых круг делался:
- действие мгновенное и необратимое, а нажать легко случайно — нужна защита от случайного нажатия;
- статус после нажатия невыразим: файл изменён, но на сайте ничего не поменяется, пока изменение не опубликовано, а публикации в программе пока нет;
- кнопка стоит рядом с «Сохранить», но живёт по обратному правилу: сохранять после неё не нужно, она применяется сразу. Отсюда вопрос владельца «нужно ли вообще сохранять» — из интерфейса это не считывается.
- **Предлагаемое направление:** видимость меняется в окне как обычная правка шапки и уезжает на диск вместе с «Сохранить». Тогда правило в программе одно на всё, а статус можно показать честно: «в меню / только по ссылке» плюс «изменение ещё не опубликовано». **Сделано ровно так 2026-08-10.**

## Веха 3 — публикация (подробности заданий В3-03 и далее)

Кнопка «Подготовить» с выбором версий галочками (в первом задании публикации галочек нет: статья едет целиком, отход назван в `DECISIONS.md` и закрывается кругом после публикации) · порядок подготовки: проверка, затем исправление механического, затем отчёт (в первом задании исправление механического приходит последним кругом, см. `CURRENT_TASK.md`) · каждая строка отчёта ведёт к месту в статье · статус «готова к публикации» ставит программа · кнопка «Опубликовать» неактивна без подготовки · правка после подготовки возвращает статью в черновик · повтор при ошибке на стороне GitHub, отчёт при ошибке на нашей · перевод ручных мелочей публикации в машинную часть · ~~переключение видимости «в меню» и «только по ссылке»~~ (сделано кругами 1 и 1б: видимость — обычное поле свойств открытой языковой версии) · управление разделами сайта: переименование, порядок, создание с заглушкой, перенос статьи с переадресацией, названия разделов во всех трёх языках · создание служебных страниц разделов (их правка работает уже в вехе 1) · удаление статьи отдельным действием с предупреждением.

## Веха 4 — языки и сотрудник

Создание статьи в любой локали с попаданием в правильное место сайта · английская заглушка для статьи, живущей пока на одном языке · плашка «перевод возможно устарел» · вход сотрудника, работа без ИИ · история действий: кто, что, когда.

Сюда относится вопрос владельца «как создать статью на EN/RU/ES и добавить локаль»: в вехе 1 только просмотр и правка существующих версий, создание — здесь. В интерфейсе кнопок создания нет, ложного ощущения готовой функции не создаётся.

---

## Отложено осознанно

Разрешение конфликтов с удалённым репозиторием · окно настроек поверх `settings.json` · сжатие картинок · дописывание строки в реестр статей базы знаний · одновременная работа владельца и сотрудника над одной статьёй · массовые действия в реестре — владелец про них не говорил, а галочки съедают ширину таблицы.
