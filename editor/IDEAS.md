# Копилка идей по коду и архитектуре

> Идеи об улучшении устройства программы, родившиеся по ходу работы. Одна строка на идею,
> дописывается в конец. Здесь не делается ничего: разбор — на границе вехи, решением владельца.
> Функции и поведение сюда не пишутся — им место в замечаниях владельца и `PLAN.md`.
> Принятая идея переезжает в `PLAN.md` (порядок) и `DECISIONS.md` (почему) и отсюда удаляется:
> второго источника правды копилка не держит. Так уехал «номер захода» (2026-08-09).

- 2026-08-09 (claude) **Сторожевой тест на сравнение текстов.** Запретить в коде окна и ручек прямое `===` между текстом окна и текстом файла: сравнение только через `nothingChanged` (SPEC 3.6). Дефект с переводами строк всплывал уже дважды.
- 2026-08-09 (claude) **Списки полей из `Properties.tsx` — в настройки.** `ДЛИННЫЕ`, `НЕПРАВИМЫЕ` и поля-картинки заданы в коде; три списка подряд — повод вынести (SPEC 4.4). Помечено и в `CURRENT_TASK.md` В3-01Д.
- 2026-08-09 (claude) **Отправка черновика при закрытии вкладки.** Хвост автосохранения из В1-13: закрытие окна раньше таймера теряет секунды набора. Техника — отправка при событии закрытия; отдельное задание.
