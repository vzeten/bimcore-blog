# CLAUDE.md — bimcore-blog (репозиторий кода)

Это репозиторий кода сайта **learn.bimcore.one** (Docusaurus).

## ⛔ DANGEROUS!!!!!!! Зависимости сайта

Никогда не удалять, не очищать и не заменять `node_modules` сайта целиком или частично без
предварительного сообщения владельцу с точной пометкой **`DANGEROUS!!!!!!!`** и его явного
согласия. Это относится также к `npm ci`, `npm prune`, ручной очистке, переносу и любому сценарию,
который сначала удаляет установленные пакеты. До согласия разрешены только проверки без записи.
Если зависимости отсутствуют или повреждены, остановиться, сообщить владельцу и не использовать
чужой `node_modules`, временный `NODE_PATH` или другую рабочую папку как обход.

Базу знаний и этот репозиторий ведут **два помощника — Claude Code и Codex** — по одним правилам; вход для Codex — `AGENTS.md` рядом (короткая ссылка сюда).

## ⚠️ База знаний проекта — в Obsidian, НЕ в этом репозитории

Вся проектная память (решения, задачи, архитектура, лог, vision) живёт в **Obsidian vault** и доступна **только через MCP obsidian** (`mcp__obsidian__*`), а НЕ через файлы этого репозитория.

- **Vault root:** `C:\My_brain` (реальный путь на диске — `C:\My_brain\My brain\` с пробелом; MCP резолвит сам).
- **НЕ ищи** `CLAUDE.md`, `_index`, `tasks` в этом репозитории через Read/Glob — их здесь нет.

### Стартовое чтение базы (через `mcp__obsidian__vault_read`)

Когда нужно «свериться с базой» / понять проект — читай в таком порядке (≤30 KB):
1. `CLAUDE.md` (в vault, корень) — схема ведения базы знаний
2. `it/web/learn/_index.md` — карта проекта, статус
3. `it/web/learn/tasks-active.md` — карта заголовков, затем карточка по теме; для редактора — `#70`
4. 1-2 целевых фрагмента по теме сессии (`vault_read` с `target=heading`)

### Как вести базу

Все правила (триггеры, шаблоны, автоведение, SPEC-слой) — в vault: `CLAUDE.md` + `meta/operations.md` + `meta/principles.md`. Следуй им.

Коротко: во время работы всё новое фиксируй одной строкой в корневом `_inbox.md` базы (дата, автор, домен, суть), статус текущей задачи — сразу в её карточке; раскладка по постоянным файлам — на «гг» (канон — принцип 18 в `meta/principles.md`). Критерии текущей операции Codex выводит из согласованного результата; вопрос нужен только при существенной неопределённости.

## Код этого репозитория

Перед составлением и правкой статей обязательно читать через Obsidian MCP `it/web/learn/content-quality-standard.md`, включая правила FAQ, CTA и смысловых блоков, и применять `.agents/skills/bimcore-content-editor/SKILL.md`. Перед передачей проверять фактическую разметку статьи по этому стандарту, а не только синтаксис MDX. Использование штатных компонентов сверять с их текущей реализацией и регистрацией в `src/theme/MDXComponents.js`; не заменять их самодельным HTML.

Docusaurus-проект. Контент и код живут в этом репозитории. Детали архитектуры сайта — в базе знаний (`it/web/learn/architecture` через MCP obsidian).

### ⛔ Ветки и папки — одна схема (решение владельца 2026-09-12)

Проверка порядка: `npm run repo:check` (файл `scripts/repo-check.mjs`). Помощник запускает её в начале
сессии и перед отчётом; красный итог означает «сначала убрать лишнее, потом работать». У Claude она
запускается сама при старте сессии (хук в `.claude/settings.json`).

**Запрос «статья» (создать, поправить, перевести, картинки):**
1. Работа только в `C:\My_code\bimcore-blog` на `main`. Ветку, копию репозитория и кэш не создавать;
   основную папку с `main` не переключать.
2. Файл пишется сразу на своё место (`blog/…`, `docs/…`, `i18n/…`, картинки рядом со статьёй).
   Доступность («Недоступно», «По ссылке», «Доступно всем») ставится только через редактор; локаль уже
   опубликованной статьи не бывает «Недоступно», только «По ссылке».
3. Результат показывается владельцу в редакторе на 4780 ссылкой на статью. Публикует владелец кнопкой
   редактора. Помощник не делает `push` статей и не коммитит их в `main`.

**Запрос «код редактора» (доработать, починить):**
1. Ветка `feature/editor-<суть>` или `fix/editor-<суть>` только от `editor`; одна временная копия
   `editor/.coordination/worktrees/<имя ветки>`; порядок операции — `editor/TASKS.md`, «Действующий процесс».
2. После «принято» владельца: перенос в `editor` (`--ff-only` в `accepted-editor`), затем в той же
   операции удаление временной копии и ветки, до отчёта. Отчёт без строки «копия и ветка удалены,
   repo-check зелёный» не считается законченным.
3. `editor` переносится в `main` только по отдельному слову владельца: слить, собрать сайт, проверить
   цену в карточке товара, отправить.

**Всегда:** копий ровно две (основная на `main`, `accepted-editor` на `editor`) плюс не больше одной
временной; веток не больше `main`, `editor`, `backup/*` и одна рабочая. Помощник не заканчивает
сессию с лишней копией, лишней веткой, незакоммиченными правками кода или висящими серверами редактора.
Новая ветка или копия вне этих правил — только по слову владельца. Страховки `backup/2026-09-12-main-folder`
и `refs/backup/2026-09-12/*` убрать после месяца спокойной работы.

Перед составлением и правкой статей обязательно читать через Obsidian MCP `it/web/learn/content-quality-standard.md`, включая правила FAQ, CTA и смысловых блоков, и применять `.agents/skills/bimcore-content-editor/SKILL.md`. Перед передачей проверять фактическую разметку статьи по этому стандарту, а не только синтаксис MDX. Использование штатных компонентов сверять с их текущей реализацией и регистрацией в `src/theme/MDXComponents.js`; не заменять их самодельным HTML.

Docusaurus-проект. Контент и код живут в этом репозитории. Детали архитектуры сайта — в базе знаний (`it/web/learn/architecture` через MCP obsidian).

### ⛔ Ветки и папки — одна схема (решение владельца 2026-09-12)

- **`main` — статьи.** Папка `C:\My_code\bimcore-blog` всегда стоит на `main`. Статьи, их картинки и
  черновики создаются и правятся только здесь. Публикацию делает владелец кнопкой редактора; кнопка
  отказывает, если папка не на `main`.
- **`editor` — код редактора.** Единственная постоянная рабочая копия этой ветки —
  `editor/.coordination/worktrees/accepted-editor`; из неё запущен редактор на 4780 (код оттуда,
  статьи из основной папки). Дочерние ветки `feature/editor-*` и `fix/editor-*` либо вливаются в
  `editor`, либо удаляются. Папка `editor/` в `main` отстаёт от ветки `editor` до следующего слияния.
- **Других копий репозитория быть не должно:** ни в `editor/.coordination/worktrees`, ни в `.codex`,
  ни во временных папках, ни в папках владельца. Временная копия для одной операции допустима одна и
  удаляется до отчёта. Кэш-копии редактора (`.cache-*`) запрещены.
- **Помощник не заканчивает работу** с несохранёнными правками, с основной папкой не на `main` и
  с висящими серверами редактора из удалённых копий. Новая ветка или копия — только по слову владельца.
- **Страховки от 2026-09-12:** ветка `backup/2026-09-12-main-folder` и ссылки `refs/backup/2026-09-12/*`
  (концы всех удалённых веток и остатки копий). Убрать после месяца спокойной работы.

### Редактор статей

Вход — [состояние процесса](editor/process/README.md) и `editor/CURRENT_TASK.md`.
[Действующий процесс](editor/TASKS.md#действующий-процесс) — единственный порядок отбора работы,
ролей, проверок по риску и завершения. Верхний Codex не дублирует технический аудит.
Функциональный код пишет Claude; прямое отдельное поручение владельца может изменить исполнителя.

`BUSINESS.md` хранит утверждённый смысл продукта, `SPEC.md` — технические свойства,
`MAP.md` — указатели на код. Открывать только фрагменты по операции.
`PLAN.md`, `MVP.md`, `DECISIONS.md`, `JOURNAL.md` и `IDEAS.md` — справочная история,
не очередь; читать по точной необходимости, а не для восстановления всей биографии проекта.

Навыки служат входами в отдельные действия: `editor-change`, `codex-gate`,
`editor-live-check`, `model-council`. Новый набор правил вокруг них не создавать.
Запуск видимой Claude-сессии, статусы `run-status.json` и ожидание — раздел «Передача между чатами и сессиями» в `editor/TASKS.md`; отдельной программы ожидания нет.
После остановки нужен короткий отчёт по [REPORT.md](editor/REPORT.md).

**Секрет Obsidian.** Bearer token не выводить, не сохранять и не пересказывать.

### ⛔ ОБЯЗАТЕЛЬНО перед любой правкой frontmatter / структуры статей

Сначала прочитать чеклист **«⛔ ЖЁСТКИЕ ИНВАРИАНТЫ»** в начале `it/web/learn/architecture.md` (через MCP obsidian). Это единое место со всеми требованиями (frontmatter-валидация, связь статей с файлами, i18n, дисциплина dev-сервера). Нарушение = падает сборка всего сайта. Не действовать «по памяти» — открыть и свериться.

### ⛔ ОБЯЗАТЕЛЬНО перед правкой `sidebars.js` / `docusaurus.config.js` / категорий / git-push / деплоем

Сначала прочитать **`it/web/learn/runbook-site-ops.md`** (через MCP obsidian) — операционный чеклист по коду сайта: дисциплина git/деплоя (один заход = один push), sidebar/категории/landing-обложки, config/плагины, локализация UI-строк, сборка на Windows, плюс журнал граблей. Цель — не ломать сайт решениями на ходу. Контент-публикация статей → `playbook-publish.md`.

### ⛔ Браузерный плагин (Claude in Chrome / Comet) — только крайнее средство

Управление браузером (`mcp__Claude_in_Chrome__*`, скриншоты, клики, навигация) **запрещено для рутинных задач**. Включать его ТОЛЬКО когда:
- задачу иначе никак не решить (нужна живая сессия пользователя, File System Access, измерение в реальном UI), ИЛИ
- альтернатива явно дороже по ресурсам/времени.

Сначала всегда пробовать дешёвые способы: чтение файлов, `curl`/`Invoke-WebRequest`, лог dev-сервера, поиск по коду/докам. Проверка «работает ли сайт / есть ли ошибка» = HTTP-запрос + лог сервера, НЕ браузер. Если кажется, что нужен браузер — сперва спросить себя, нет ли способа без него, и при сомнении спросить пользователя.

**Запрет не касается редактора статей.** Живая проверка программы во встроенном окне на `localhost:4780` обязательна — порядок в навыке `editor-live-check`. Запрет выше относится к внешнему браузеру с вашей живой сессией (Claude in Chrome, Comet) для рутинных задач и его не отменяет.
